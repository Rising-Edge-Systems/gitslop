# Ralph Progress Log
Started: 04/13/2026 21:23:59
---

## Codebase Patterns
- IPC handlers for GitHub/GitLab APIs live in `src/main/index.ts`, use `githubApiRequest`/`gitlabApiRequest` helpers
- Preload exposes IPC to renderer in `src/preload/index.ts` under `github`/`gitlab` objects
- Type definitions for `window.electronAPI` are in `src/renderer/src/hooks/useLayoutState.ts`
- GitHub label colors come WITHOUT `#` prefix — add `#` when mapping
- GitHub issues endpoint returns PRs too — filter with `!item.pull_request`
- Use `npm run typecheck` (not bare `tsc --noEmit`) for project typecheck
- Tests: `npm test` runs vitest

## 2026-04-13 21:31 - US-IS-001
- Implemented github:listIssues and github:getIssue IPC handlers
- Added preload exposure and type definitions
- Files changed: src/main/index.ts, src/preload/index.ts, src/renderer/src/hooks/useLayoutState.ts
- **Learnings for future iterations:**
  - Follow existing PR handler patterns exactly — same error handling, same githubApiRequest helper
  - GitHub issues API returns PRs in results — must filter with `!item.pull_request`
  - Label color from GitHub has no `#` prefix — prepend it when mapping
  - getIssue fetches issue + comments in parallel with Promise.all
---

## 2026-04-13 21:35 - US-IS-002
- Implemented gitlab:listIssues and gitlab:getIssue IPC handlers
- Added preload exposure and type definitions
- Files changed: src/main/index.ts, src/preload/index.ts, src/renderer/src/hooks/useLayoutState.ts
- **Learnings for future iterations:**
  - GitLab issues follow same pattern as MR handlers — getGitLabConfig, gitlabApiRequest, same auth/error pattern
  - GitLab state mapping for issues: 'open' -> 'opened', 'closed' -> 'closed' (simpler than MR which has 'merged')
  - GitLab labels in list endpoint are plain strings (not objects) — map as { name: l }
  - GitLab issue comments are "notes" — filter system notes with n.system check
  - getIssue fetches issue + notes in parallel with Promise.all (same pattern as GitHub getIssue)
---

## 2026-04-13 21:40 - US-IS-003
- Implemented GitHubIssuesSection component in Sidebar.tsx, rendered below PullRequestsSection
- Added interfaces: GitHubIssue, GitHubIssueDetail, GitHubIssuesSectionProps
- Features: login check, GitHub remote detection, open/closed filter tabs, issue list with labels and comment count, detail overlay with author avatar, labels, body, comments, Open in GitHub button, close/backdrop dismiss, refresh button
- Files changed: src/renderer/src/components/Sidebar.tsx (added MessageSquare import, ~270 lines of new component code)
- **Learnings for future iterations:**
  - GitHub issue label colors from IPC handlers already include `#` prefix — use directly in CSS
  - Reuse existing PR CSS classes (prFilterBtn, prItem, prDetailOverlay, etc.) for issue sections — no new CSS needed
  - The ghInfo parsing pattern (parseRemote + isLoggedIn) is identical across PR and Issue sections
  - Issue rows don't need branch info (unlike PRs) — simpler item layout
  - Detail overlay for issues is simpler than PRs — no reviews, no files changed, no stats
---

## 2026-04-13 21:45 - US-IS-004
- Implemented GitLabIssuesSection component in Sidebar.tsx, rendered below MergeRequestsSection
- Added interfaces: GitLabIssue, GitLabIssueDetail, GitLabIssuesSectionProps
- Features: login check, GitLab remote detection, open/closed filter tabs, issue list with labels and comment count, detail overlay with author avatar, labels, body, notes (comments), Open in GitLab button, close/backdrop dismiss, refresh button
- Also added to collapsed rail overlay under mergerequests section
- Files changed: src/renderer/src/components/Sidebar.tsx (added ~274 lines of new component code)
- **Learnings for future iterations:**
  - GitLab issue state is 'opened' (not 'open') — use 'opened' for state badge comparison
  - GitLab labels may or may not have a color field — handle both cases with conditional styling
  - GitLab issue URL pattern: {webUrl}/-/issues/{iid}
  - GitLab issues section shares glInfo pattern identically with MergeRequestsSection
  - For collapsed rail, GitLab issues section was bundled under the mergerequests rail icon since there's no separate issues icon in the rail
---
