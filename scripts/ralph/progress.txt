# Ralph Progress Log
Started: Wed Apr 9 2026
---

## Codebase Patterns
- IPC pattern: git-service.ts method -> git-ipc.ts handler -> preload/index.ts expose -> useLayoutState.ts type declaration (4 files for each new git operation)
- Existing IPC: showFileAtCommit runs `git show HASH:PATH` — follow same pattern for showFileAtParent
- DiffViewer.tsx: has InlineDiffView and SideBySideDiffView internal components, mode toggled via useState
- DiffViewMode type: currently 'inline' | 'side-by-side', exported from DiffViewer.tsx and useLayoutState.ts
- Center diff rendering: RepoView.tsx renders DiffViewer when viewingDiff=true, also handles 'file' mode with a separate fetch
- Side-by-side view: SideBySideDiffView has scroll sync between left/right panes via refs and onScroll handlers
- DiffHunk type: { header, headerSuffix, lines[] } where each line has { type: 'added'|'removed'|'context', content, oldLineNum?, newLineNum? }
- Syntax highlighting: SyntaxHighlightedContent component and highlightLine function
- CSS modules: import styles from './DiffViewer.module.css'
- sbsPane/sbsPaneInner/sbsPaneHeader classes for side-by-side pane structure
- Binary detection: check for null bytes (\0) in content
- Typecheck: npm run typecheck
- Build: npx electron-vite build && npx electron --no-sandbox .
- Center view mode in RepoView.tsx: 'diff' | 'full' | 'file' — separate from DiffViewer's inline/split toggle
- FullDiffView component is co-located in DiffViewer.tsx to share parseDiff, detectLanguage, SyntaxHighlightedContent
---

## 2026-04-09 - US-FD-001
- Implemented showFileAtParent IPC method across all 4 layers
- Files changed: git-service.ts, git-ipc.ts, preload/index.ts, useLayoutState.ts
- **Learnings for future iterations:**
  - The showFileAtParent method uses `git rev-parse HASH^1` to get parent, then `git show PARENT:PATH`
  - Error handling catches both missing parent (initial commit) and missing file (new file) — returns empty string
  - Binary detection checks for null bytes in stdout
  - Pattern is identical to showFileAtCommit — 4 files touched for each new IPC method
---

## 2026-04-09 - US-FD-002
- Implemented Full Diff View Mode — side-by-side complete files with diff highlights
- Files changed: DiffViewer.tsx, DiffViewer.module.css, RepoView.tsx
- Added 'Full' button to center view mode toggle (Diff | Full | File) in RepoView.tsx
- Added FullDiffView component in DiffViewer.tsx: fetches old (showFileAtParent) and new (showFileAtCommit) content
- Uses buildChangedLinesSets() to extract changed line numbers from parsed diff hunks
- Renders both complete files side by side with green/red highlights on changed lines
- Scroll sync between panes, line numbers, syntax highlighting on all lines
- Loading state while fetching both file versions
- **Learnings for future iterations:**
  - The centerViewMode state in RepoView.tsx controls Diff/Full/File toggle — separate from DiffViewer's inline/split mode
  - FullDiffView reuses existing SBS CSS classes (sbsPane, sbsLine, etc.) and SyntaxHighlightedContent
  - parseDiff and detectLanguage are private to DiffViewer.tsx — FullDiffView is co-located to access them
  - Columns icon from lucide-react works well for the "Full" button
  - Build validates after typecheck — run both to be safe
---
