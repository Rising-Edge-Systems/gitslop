# Ralph Progress Log
Started: Thu Apr  9 03:30:47 AM EDT 2026
---

## Codebase Patterns
- Typecheck: `npm run typecheck` (runs typecheck:node + typecheck:web)
- CommitGraph.tsx uses react-window v2 with `useListCallbackRef` for list virtualization
- CSS modules pattern: `styles.className` with `CommitGraph.module.css`
- HEAD commit identified by `refs.some(r => r.type === 'head')` — type 'head' means the current branch ref
- Lane 0 is always HEAD lane; lane colors via `color-mix()` CSS function
- Floating overlays: position absolute within `.container`, z-index 1200+ range
- `listRef.scrollToRow({ index, align })` for programmatic scrolling
- Graph nodes computed via `computeGraphLayout(commits)` → `useMemo`
- All repo-modifying git ops go through `withWatcherSuppression()` in git-ipc.ts — it handles watcher suppression + forced refresh
- `sendRepoChangedForced()` in index.ts bypasses watcher suppression for explicit repo:changed events

## 2026-04-09 - US-MP-001
- Implemented current branch visual indicator in commit graph
- Files changed: CommitGraph.tsx, CommitGraph.module.css
- **What was done:**
  - Enhanced HEAD ref badge: bolder border (2px), brighter background, font-weight 700, subtle glow shadow
  - Added `.rowHead` class: 3px left accent border on HEAD commit row with faint blue background
  - Added floating HEAD indicator that appears when HEAD is scrolled off-screen, showing "HEAD: branchname" with click-to-scroll functionality
  - Computed `headInfo` (index + branchName) via useMemo for efficient HEAD tracking
- **Learnings for future iterations:**
  - The project uses split tsconfigs: tsconfig.node.json and tsconfig.web.json — use `npm run typecheck` not bare `tsc`
  - react-window v2 `scrollToRow` takes `{ index, align }` object
  - `visibleRange` (start/stop) tracks what rows are rendered — use for off-screen detection
  - Pre-existing TS6305 errors from dist/ folder are noise; only real errors matter
---

## 2026-04-09 - US-MP-002
- Implemented automatic graph refresh after all git operations
- Files changed: src/main/index.ts, src/main/git-ipc.ts
- **What was done:**
  - Added `sendRepoChangedForced()` in index.ts: sends `repo:changed` IPC event bypassing watcher suppression (150ms delay for disk writes)
  - Modified `withWatcherSuppression()` in git-ipc.ts to call `sendRepoChangedForced()` after every successful git operation
  - All operations using `withWatcherSuppression` (merge, commit, checkout, push, pull, fetch, rebase, cherry-pick, revert, reset, stash, tag, branch ops) now trigger immediate graph refresh
  - Scroll position preserved naturally: `loadCommits` only updates data array, react-window maintains position
- **Learnings for future iterations:**
  - `withWatcherSuppression` is the central wrapper for ALL repo-modifying git ops in git-ipc.ts — modifying it affects everything
  - The watcher suppression pattern: suppresses during ops + 1s after → file change events are dropped → need explicit event after completion
  - `sendRepoChanged()` is private and checks suppression; `sendRepoChangedForced()` is exported and bypasses it
  - CommitGraph already listens to `repo:changed` via `onRepoChanged` and calls `loadCommits()` — no renderer changes needed
- Window.electronAPI type declarations are in `src/renderer/src/hooks/useLayoutState.ts` (declare global)
- electron-store StoreSchema is in `src/main/index.ts` — add new fields there
- SettingsPanel sections: add id to `SettingsSection` type union, add entry to sections array, add conditional rendering
---

## 2026-04-09 - US-MP-003
- Added merge option to commit right-click context menu
- Files changed: CommitGraph.tsx
- **What was done:**
  - Modified `buildCommitContextMenuItems` to accept `refs` and `currentBranch` parameters
  - Filters branch/remote refs excluding current branch to build merge menu items
  - Each mergeable branch shows "Merge [branchname] into [current branch]" with GitMerge icon
  - Added `mergeBranch` state to CommitGraph; clicking merge opens MergeDialog pre-filled with selected branch
  - After merge completes, graph refreshes via `loadCommits()`
  - Updated `handleContextAction` to accept optional `extra` string param for branch name
- **Learnings for future iterations:**
  - `buildCommitContextMenuItems` now has 5 params (commit, multiSelectCount, onAction, refs, currentBranch)
  - `handleContextAction` signature now includes optional `extra?: string` for passing branch names
  - MergeDialog accepts `preselectedBranch` prop and renders its own overlay — safe to mount anywhere
  - `headInfo?.branchName` gives current branch name from the graph data
---

## 2026-04-09 - US-MP-004
- Redesigned MergeDialog with clean, polished UI — fully self-contained CSS module
- Files changed: MergeDialog.tsx, MergeDialog.module.css, git-service.ts, git-ipc.ts, preload/index.ts
- **What was done:**
  - Replaced all global `branch-dialog-*` classes with self-contained CSS module styles
  - Proper modal overlay (centered, 480px max-width, var(--space-lg) padding, slide-up animation)
  - Title: "Merge into [current branch]" with GitMerge icon and accent-colored branch name
  - Searchable branch dropdown with filtering, selected badge with clear button
  - Preview section: commit count, fast-forward status with spinner loading
  - Options: --no-ff and --squash checkboxes (mutually exclusive) instead of radio buttons
  - Merge button: deep blue (#3b82f6), white text, font-weight 600 with spinner during merge
  - Cancel button: subtle secondary style
  - Success state: CheckCircle2 icon with pop-in animation, auto-close after 1.2s
  - Error state with AlertTriangle icon
  - Conflict state with file list, abort button, hints
  - Added --squash support to git-service.ts, git-ipc.ts, and preload/index.ts
- **Learnings for future iterations:**
  - MergeDialog now uses ONLY CSS module classes — no global branch-dialog dependencies
  - The `squash` option was added to the merge IPC: `opts.squash` in git-ipc.ts and git-service.ts
  - MergeDialog uses searchable dropdown pattern (searchQuery + filteredBranches + dropdownOpen state) — reusable for other dialogs
  - noFf and squash are mutually exclusive — toggling one off the other
---

## 2026-04-09 - US-MP-005
- Enhanced commit right-click context menu with full GitKraken-like actions
- Files changed: CommitGraph.tsx, ResetDialog.tsx
- **What was done:**
  - Split single "Reset" into 3 items: Reset Soft, Reset Mixed, Reset Hard — each opens ResetDialog pre-selected to that mode
  - Reset Hard marked as `danger: true` — renders in red via existing `.itemDanger` CSS
  - Added "Copy commit message" action (copies subject + body)
  - Added "Rebase current onto [branch]" for commits with branch refs (uses existing `git.rebase` IPC)
  - Added proper separators between action groups: checkout | cherry-pick/revert | resets | merge/rebase | create branch/tag | copy actions
  - ResetDialog now accepts optional `defaultMode` prop to pre-select the reset mode
  - Imported `MessageSquare` and `GitPullRequestArrow` icons from lucide-react
- **Learnings for future iterations:**
  - `ContextMenu` component auto-inserts separators before `danger: true` items if none exists
  - `git.rebase(repoPath, ontoBranch)` IPC already available in preload — no backend changes needed for basic rebase
  - `commit.body` contains the full commit message body (separate from `commit.subject`)
  - ResetDialog uses type confirmation ("HARD") for hard resets — safe UX pattern
---

## 2026-04-09 - US-MP-006
- Implemented Profiles feature: create, edit, delete, switch author identities
- Files changed: index.ts, preload/index.ts, App.tsx, SettingsPanel.tsx, SettingsPanel.module.css, StatusBar.tsx, StatusBar.module.css, useLayoutState.ts
- **What was done:**
  - Added Profile interface and electron-store schema (profiles array + activeProfileId) in index.ts
  - Created 7 IPC handlers: profiles:list, profiles:getActive, profiles:create, profiles:update, profiles:delete, profiles:setActive, profiles:apply
  - profiles:apply sets git config user.name and user.email on the specified repo
  - Exposed profiles IPC namespace in preload/index.ts with full type definitions
  - Added ProfileData type to Window.electronAPI in useLayoutState.ts
  - Added "Profiles" section to SettingsPanel with full CRUD UI: profile cards, edit form, create form
  - Default profile selection, active profile badge, inline editing
  - Profile indicator in StatusBar right section showing active profile name with UserCircle icon
  - Styled with CSS module: profile cards, active/default badges, form controls, action buttons
  - Passed currentRepo prop through SettingsPanel for apply-to-repo functionality
- **Learnings for future iterations:**
  - Window.electronAPI type declarations live in useLayoutState.ts `declare global { interface Window { ... } }`
  - electron-store schema is in src/main/index.ts StoreSchema interface
  - SettingsPanel sections defined as array of {id, label, icon} objects — add new id to SettingsSection type union
  - Profile IPC doesn't use withWatcherSuppression since git config changes don't affect file watcher
  - SettingsPanel accepts optional currentRepo prop for features that need repo context
---

## 2026-04-09 - US-MP-007
- Implemented SSH Key Management feature in Settings panel
- Files changed: index.ts, preload/index.ts, SettingsPanel.tsx, SettingsPanel.module.css, useLayoutState.ts
- **What was done:**
  - Added 5 IPC handlers in main process: sshkeys:list, sshkeys:readPublicKey, sshkeys:copyToClipboard, sshkeys:generate, sshkeys:testConnection
  - sshkeys:list scans ~/.ssh/ for .pub files and their matching private keys, reads fingerprints via ssh-keygen -l
  - sshkeys:generate uses child_process.exec to run ssh-keygen with Ed25519 or RSA 4096-bit types
  - sshkeys:testConnection runs `ssh -T` against a given host and parses output for authentication success
  - Exposed sshkeys namespace in preload with full type definitions
  - Added 'sshkeys' to SettingsSection type union and sections array with KeyRound icon
  - SSHKeysSection component: lists existing keys with type badge and fingerprint, copy public key button, generate new key form, test SSH connection UI
  - Full CSS module styles following existing patterns (sshKeyCard, sshKeyTypeBadge, sshTestSection, etc.)
- **Learnings for future iterations:**
  - `clipboard` from electron main process used for sshkeys:copyToClipboard — imported from `electron`
  - ssh-keygen -l -f <pubkey> returns: `bits fingerprint comment (TYPE)` — parse TYPE from parens
  - `ssh -T git@github.com` returns exit code 1 even on success — must catch the error and parse stdout/stderr
  - child_process.exec needs promisify for async/await usage in main process
  - SSHKeyInfo interface: { name, path, pubKeyPath, type, fingerprint }
---

## 2026-04-09 - US-MP-008
- Implemented commit signing configuration per profile
- Files changed: index.ts, preload/index.ts, useLayoutState.ts, SettingsPanel.tsx, SettingsPanel.module.css
- **What was done:**
  - Added `signingMethod`, `gpgKeyId`, `sshKeyPath` fields to Profile interface and ProfileData
  - Updated `profiles:apply` IPC to set `commit.gpgsign`, `gpg.format`, `user.signingkey` git config based on profile signing method
  - GPG signing: sets format to `openpgp` with selected GPG key ID
  - SSH signing: sets format to `ssh` with selected SSH public key path
  - None: disables signing and unsets format/signingkey
  - Profile edit form now shows a Signing section with None/GPG/SSH toggle buttons
  - GPG mode: loads available GPG keys via existing `getAvailableGpgKeys` IPC and shows dropdown
  - SSH mode: loads SSH keys via existing `sshkeys:list` IPC and shows dropdown
  - Profile cards show a green signing badge (GPG/SSH) when signing is configured
  - Updated Window.electronAPI types in useLayoutState.ts for new profile fields
  - Added CSS styles: signingSection, signingMethodSelector, signingMethodBtn, signingKeySelect, profileSigningBadge
- **Learnings for future iterations:**
  - `git config --unset` throws an error if the config key doesn't exist — wrap in try/catch
  - GPG signing uses `gpg.format = openpgp`, SSH signing uses `gpg.format = ssh` (yes, the config key is still `gpg.format` even for SSH)
  - SSH signing key path should point to the `.pub` file
  - Reused `sshSpinner` CSS class from SSH keys section for loading states
---

## 2026-04-09 - US-MP-009
- Implemented GitHub integration with PAT-based authentication
- Files changed: index.ts, preload/index.ts, useLayoutState.ts, SettingsPanel.tsx, SettingsPanel.module.css
- **What was done:**
  - Added `safeStorage` import from electron for secure token encryption/decryption
  - Created `githubApiRequest()` helper for HTTPS requests to GitHub API (using built-in `https` module)
  - Created `encryptToken()`/`decryptToken()` helpers using `safeStorage.encryptString()`/`decryptString()` with base64 encoding, falls back to plain storage if encryption unavailable
  - Added 4 IPC handlers: `github:login` (validates PAT via GET /user), `github:getUser`, `github:logout`, `github:isLoggedIn`
  - Extended StoreSchema with `githubToken`, `gitlabToken`, `gitlabInstanceUrl` fields
  - Added `github` namespace in preload with login/getUser/logout/isLoggedIn methods
  - Added `github` types in Window.electronAPI declarations
  - Added 'github' to SettingsSection type union, sections array with Github icon
  - Created GitHubSection component: login form with PAT input (show/hide toggle), login button, error display, security note
  - Logged-in state shows: avatar image, display name, @username, email, "View Profile" link, logout button
  - Full CSS module styles: ghUserCard, ghAvatar, ghLoginBtn (GitHub dark #24292e), ghTokenField, ghError, ghSecurityNote
- **Learnings for future iterations:**
  - `safeStorage.isEncryptionAvailable()` must be checked before encrypt/decrypt — not always available
  - `safeStorage.encryptString()` returns a Buffer — store as base64 string in electron-store
  - GitHub API requires `User-Agent` header — any string works (used 'GitSlop')
  - Built-in `https` module works well for API calls — no need for external dependencies
  - `Github` icon is available from lucide-react (capital G)
  - StoreSchema was extended with gitlabToken and gitlabInstanceUrl proactively for US-MP-011
---

## 2026-04-09 - US-MP-010
- Implemented GitHub Pull Requests feature: view and create PRs from sidebar
- Files changed: index.ts, preload/index.ts, useLayoutState.ts, Sidebar.tsx, Sidebar.module.css
- **What was done:**
  - Added `parseGitHubRemote()` helper to extract owner/repo from remote URLs (SSH + HTTPS)
  - Added 4 IPC handlers: `github:parseRemote`, `github:listPullRequests`, `github:getPullRequest`, `github:createPullRequest`
  - `listPullRequests` fetches open/closed PRs with title, number, author, labels, draft status, branch info
  - `getPullRequest` fetches full details including comments, reviews, and files changed in parallel
  - `createPullRequest` creates new PR with title, body, head/base branches, draft option
  - Exposed all new IPC methods in preload and Window.electronAPI types
  - Created `PullRequestsSection` component in Sidebar with:
    - Auto-detection of GitHub remotes (only shows section when GitHub remote detected)
    - Login check (shows hint if not logged in)
    - Open/Closed filter toggle with refresh button
    - PR list with number, title, author, time ago, draft badge, labels, branch info
    - Click-to-expand PR detail overlay with: description, reviews (with approval/changes_requested states), comments, files changed (with A/D/M/R status indicators), stats (+/-/files)
    - "Open in Browser" button for each PR
    - Create PR dialog: title, description, head/base branch selectors, draft checkbox
    - Success state after creation with link to open in browser
  - Added 'pullrequests' to RailSection type and RAIL_SECTIONS array
  - Full CSS module styles following existing design token patterns
- **Learnings for future iterations:**
  - `SkeletonList` does not accept a `height` prop — only `count`, `showIcon`, `varyWidths`, `className`
  - `parseGitHubRemote` uses regex: `/github\.com[:/]([^/]+)\/(.+?)(?:\.git)?$/` — works for both SSH and HTTPS
  - GitHub API GET /repos/OWNER/REPO/pulls returns PR list; GET /pulls/:number for details
  - PR reviews are at /pulls/:number/reviews, PR comments at /issues/:number/comments, files at /pulls/:number/files
  - `window.open(url, '_blank')` works in Electron renderer for opening external links
  - RailSection type must be updated when adding new sidebar sections (union type + RAIL_SECTIONS array)
---

## 2026-04-09 - US-MP-011
- Implemented GitLab integration: PAT login, user info display, merge requests (list, detail, create)
- Files changed: index.ts, preload/index.ts, useLayoutState.ts, SettingsPanel.tsx, Sidebar.tsx
- **What was done:**
  - Added `gitlabApiRequest()` helper supporting configurable instance URL (self-hosted + gitlab.com) with both HTTPS and HTTP
  - Created `parseGitLabRemote()` to extract project path from SSH/HTTPS remote URLs, matching against configured instance hostname
  - Added 9 IPC handlers: gitlab:login, gitlab:getUser, gitlab:logout, gitlab:isLoggedIn, gitlab:getInstanceUrl, gitlab:parseRemote, gitlab:listMergeRequests, gitlab:getMergeRequest, gitlab:createMergeRequest
  - Token stored securely via existing `encryptToken`/`decryptToken` helpers using safeStorage
  - GitLab Settings section with instance URL field (defaults to gitlab.com), PAT input, user card display
  - MergeRequestsSection in Sidebar: auto-detects GitLab remotes, shows open/merged MR list, detail overlay with description/comments/files, create MR dialog
  - Added 'gitlab' to SettingsSection type and 'mergerequests' to RailSection type
  - Reused all existing PR CSS styles (prItem, prFilterRow, prDetailOverlay, etc.) for consistent UI
- **Learnings for future iterations:**
  - GitLab API uses `PRIVATE-TOKEN` header (not `Authorization: token`)
  - GitLab uses URL-encoded project path (`encodeURIComponent(projectPath)`) instead of owner/repo
  - GitLab MR numbers shown as `!123` (not `#123` like GitHub PRs)
  - GitLab API `state` for open MRs is `opened` (not `open`), and `merged` (not `closed`)
  - GitLab labels in MR list are plain strings, not objects with color — style differently
  - `gitlabApiRequest` needs to handle both http and https for self-hosted instances
  - GitLab changes endpoint returns diff data but not per-file line counts like GitHub
---

## 2026-04-09 - US-MP-012
- Implemented Interactive Rebase UI with drag-to-reorder, polished modal, and full lifecycle support
- Files changed: CommitGraph.tsx, RebaseDialog.tsx, RebaseDialog.module.css
- **What was done:**
  - Completely redesigned RebaseDialog with self-contained CSS module (no global branch-dialog classes)
  - Added drag-to-reorder for interactive rebase commits using HTML5 drag-and-drop
  - Searchable branch dropdown (same pattern as MergeDialog) replacing plain <select>
  - Each commit row shows action dropdown (pick/reword/squash/fixup/edit/drop) with color-coded borders
  - Grip handle icon (GripVertical) for drag affordance, drag-over indicator with accent border
  - Dropped commits visually struck through with reduced opacity
  - Progress bar during in-progress rebase (fills based on current/total steps)
  - Success state with CheckCircle2 animation and auto-close after 1.2s
  - Full conflict resolution UI with progress badge, file list, continue/skip/abort buttons
  - Published commits warning for rebasing pushed history
  - Added "Interactive rebase onto [branch]" to CommitGraph commit context menu (RotateCcw icon)
  - Changed "Rebase current onto [branch]" context menu to open RebaseDialog instead of direct git rebase
  - Added `startInteractive` prop to open dialog directly in interactive mode
- **Learnings for future iterations:**
  - HTML5 drag-and-drop with `draggable`, `onDragStart/Over/End` works well for commit reordering
  - `GripVertical` from lucide-react is the standard drag handle icon
  - `git rebase -i` with `GIT_SEQUENCE_EDITOR` env var is how interactive rebase is automated
  - RebaseDialog now uses same modal pattern as MergeDialog (overlay/dialog/header/body/footer)
  - The `rebaseInteractive` IPC sends actions as `{hash, action}[]` — order matters for reordering
---
