# Ralph Progress Log
Started: Fri Apr 10 04:03:27 PM EDT 2026
---

## Codebase Patterns
- Tests live in `src/main/__tests__/*.test.ts` using vitest; run with `npm test` or `npx vitest run <file>`.
- Quality gates: `npm run typecheck` (node + web) and `npm test`. Both must be green before committing.
- Main-process code uses Node built-ins freely (crypto, http) — avoid new deps.
- Main process entry: `src/main/index.ts`. New main-process modules should be siblings of index.ts.
- Fixed loopback OAuth redirect: `http://localhost:47823/callback` (see `src/main/gitlab-oauth.ts` `LOOPBACK_PORT`). Do not change — it is baked into user-registered GitLab OAuth apps.

---

## 2026-04-10 - US-GL-001
- Implemented PKCE helpers and loopback callback server for GitLab OAuth.
- Files created:
  - `src/main/gitlab-oauth.ts` — exports `generatePkcePair()`, `generateState()`, `startLoopbackServer()`, `LOOPBACK_PORT` (47823), `LOOPBACK_REDIRECT_URI`.
  - `src/main/__tests__/gitlab-oauth.test.ts` — 10 unit tests covering PKCE correctness, URL-safety, loopback happy path, EADDRINUSE, and ?error= handling.
- Quality: typecheck clean, all 121 tests pass (6 files).
- **Learnings for future iterations:**
  - `base64url` digest encoding works on modern Node but we normalize manually in `toBase64Url()` for safety.
  - Loopback server must give the browser ~500ms after responding before closing, otherwise the user sees a "connection reset" page.
  - For EADDRINUSE tests, clean up the first server by firing a real callback to it — do not leak listeners.
  - GitLab OAuth redirect URI must be pre-registered, so `LOOPBACK_PORT` is a hard-coded constant that must match the Settings UI instructions (US-GL-004).
---
