# Ralph Progress Log
Started: Wed Apr 9 2026
---

## Codebase Patterns
- CommitGraph.tsx: ~2000 lines, renders virtualized commit rows with SVG nodes and ref badges
- Context menus: built with ContextMenu component, items are arrays of { key, label, icon, onClick, danger? }
- Commit context menu: buildCommitContextMenuItems() function returns ContextMenuEntry[]
- Branch ref context menu: handleRefContextMenu() builds items for branch/tag/remote refs
- MergeDialog.tsx: 360 lines, uses global branch-dialog CSS classes (needs CSS module migration)
- Git operations IPC: git-service.ts -> git-ipc.ts -> preload -> type declarations
- File watcher: onRepoChanged fires when files change in repo, debounced listeners in components
- electron-store: used for recent repos, can store profiles/tokens via store.get/set in main process
- electron safeStorage: for encrypting sensitive data (tokens, keys) before storing
- Settings: useSettings() hook, SettingsPanel.tsx with tabs
- StatusBar.tsx: shows branch, ahead/behind, fetch status — can add profile indicator
- GitHub API: https://api.github.com, needs Authorization: token TOKEN header
- GitLab API: https://INSTANCE/api/v4, needs PRIVATE-TOKEN header
- SSH keys: ~/.ssh/ directory, ssh-keygen for generation, ssh -T for testing
- GPG keys: existing getAvailableGpgKeys IPC handler, git config commit.gpgsign/user.signingkey
- Typecheck: npm run typecheck
- Build: npx electron-vite build && npx electron --no-sandbox .
- CSS modules: import styles from './Component.module.css'
- Design tokens: --accent (#89b4fa), --border, --bg-secondary, --bg-tertiary, --text-muted
- Commit button color: #3b82f6 (deep blue) for primary actions
---
