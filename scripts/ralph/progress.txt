# Ralph Progress Log
Started: Mon Mar 16 11:36:31 PM EDT 2026
---

## Codebase Patterns
- Renderer components are in `src/renderer/src/components/`
- Use `lucide-react` for icons: `import { IconName } from 'lucide-react'` with `size` and `className` props
- Toolbar emoji icons use `<span className="toolbar-btn-icon">` wrapper pattern
- Typecheck: `npm run typecheck` (runs both node and web)
- PRD branch is `ralph/ui-overhaul`, created from `ralph/gitslop-v1`
- SidebarSection `icon` prop is `React.ReactNode` — pass icons as `icon={<IconName size={16} />}`
- Context menu icons: 14px, section header icons: 16px, inline small icons: 10-12px, status bar icons: 14px
- ContextMenuItem.icon is `React.ReactNode` — pass Lucide components like `<Pencil size={14} />`
- All emoji/unicode icons replaced with Lucide as of US-042c — zero emoji/unicode remaining in renderer
- `→` in description strings and `&#123;`/`&#125;` for JSX curly braces are NOT icons — leave them
- Design tokens in `src/renderer/src/styles/tokens.css` — use `--space-{xs,sm,md,lg,xl,2xl}`, `--font-{xs,sm,md,lg,xl}`, `--radius-{sm,md,lg}`, `--shadow-{sm,md,lg}`, `--transition-{fast,normal}` instead of hardcoded values
- tokens.css is imported BEFORE global.css in main.tsx — all color/spacing/typography vars live there
- CSS modules: `import styles from './Component.module.css'` then `className={styles.myClass}` — type declarations in `src/renderer/src/css-modules.d.ts`
- CSS module class names use camelCase (e.g., `styles.btnActive`) instead of kebab-case
- Shared dialog styles (`branch-dialog-*`) stay in global.css — don't move them into component modules
- `branch-dialog-btn-primary`, `branch-dialog-btn-secondary`, `branch-dialog-btn-danger` are shared across Toolbar, Sidebar, MergeDialog, RebaseDialog, SearchPalette
- `branch-ctx-menu-*` styles are shared across Sidebar, FileTree, ContextMenu — keep in global.css
- FileTree.tsx imports sidebarStyles for shared classes (placeholder, searchBox, searchInput, searchClear) — update FileTree when modifying sidebar styles
- ALL component styles are now in CSS modules — global.css has only reset, layout, shared branch-dialog/ctx-menu, resize handles, syn-* tokens, and slideUp animation
- Cross-component CSS module imports are fine: e.g., RepoView imports blameStyles, conflictStyles, editorStyles for child component wrappers
- File watcher: all mutating git IPC handlers are wrapped with `withWatcherSuppression()` from git-ipc.ts; watcher ignores ALL `.git/` and `node_modules/` changes
- File watcher pure logic is in `src/main/watcher-utils.ts` — testable without Electron imports
- Tests: use `// @vitest-environment happy-dom` comment for renderer tests, no vitest.config.ts needed
- Tests: install `@testing-library/react`, `@testing-library/dom`, `happy-dom` for React component/hook tests
- global.css is ~448 lines (8.5KB) — the branch-dialog/ctx-menu shared styles are the bulk
- GUITest has `hover(rel_x, rel_y)` method for moving mouse without clicking (useful for tooltip tests)
- Three-column layout: Group(vertical) > [Group(horizontal) > Sidebar | Center | DetailPanel] | Terminal — terminal spans full width
- `onCommitSelect` callback chain: AppLayout → MainContent → RepoView → CommitGraph — lifts selected commit to layout level
- CommitDetail type exported from CommitGraph.tsx — use for detail panel props
- Layout state (useLayoutState) persists sidebar, bottom panel, and right panel sizes to localStorage
- Sidebar collapse: `sidebarCollapsed` in layout state, icon rail rendered outside resizable panels as fixed 48px flex item
- Sidebar props: `collapsed: boolean` and `onToggleCollapse: () => void` — both required
- `useWindowWidth()` hook tracks window.innerWidth — use for responsive breakpoints (e.g., DETAIL_PANEL_BREAKPOINT = 1400px)
- Detail panel overlay: `overlay` prop switches between inline (resizable column) and overlay (fixed + backdrop) modes
- Lane assignment: `assignLanes()` from `laneAssignment.ts` — pure function, takes `LaneCommit[]`, returns `LaneAssignmentResult[]` with lane, color, parentConnections
- Data refresh: components use `window.electronAPI.onRepoChanged()` with debounced timeouts (500-2000ms), NOT setInterval polling
- GUI tests: `self.scroll(rel_x, rel_y, clicks, direction)` helper available for mouse wheel scrolling (X11 Button4/5)
- Loading state: only show loading spinner on initial mount (track via `initialLoadDone` ref), refresh silently in background
- In-flight guards: use refs (`refreshInFlightRef`) to prevent overlapping async calls in the same component
- MainContent is wrapped in React.memo — avoid passing new object/function references as props unless necessary
- SVG commit graph: `GraphSVG` component replaces canvas; uses `assignLanes()` from laneAssignment.ts; `node.color` is pre-computed per lane
- GraphNode uses `lane` (not `column`) and `parentConnections` (not `parents`) — aligned with laneAssignment.ts types
- All context menus use the reusable `ContextMenu` component — build items with `ContextMenuEntry[]` arrays, not custom menu components
- `branch-ctx-menu-*` CSS has been removed from global.css — all context menus now use ContextMenu.module.css styles

## 2026-03-16 - US-041
- Installed lucide-react (^0.577.0) as a dependency
- Replaced settings gear emoji (⚙) in Toolbar.tsx with Lucide `Settings` icon (size=18, className="lucide-icon")
- Files changed: package.json, package-lock.json, src/renderer/src/components/Toolbar.tsx
- **Learnings for future iterations:**
  - lucide-react icons accept `size` and `className` props directly
  - Toolbar buttons use `<span className="toolbar-btn-icon">` as a wrapper for icons
  - The SettingsPanel.tsx also has a gear emoji (line 36) in its tab config — will need updating in US-042c
  - FileTree.tsx has unicode gear symbols for yml/rs file types — these are file type indicators, not UI icons
---

## 2026-03-16 - US-042a
- Replaced all emoji/unicode icons in Toolbar.tsx with Lucide icons: ArrowDownToLine (pull), ArrowUpFromLine (push), RefreshCw (fetch), GitBranch (branch), GitMerge (merge), Archive (stash), Loader2 (spinner), AlertTriangle (warning), Check/XCircle (notifications)
- Replaced all emoji/unicode icons in TitleBar.tsx with Lucide icons: Sun/Moon (theme toggle), Minus (minimize), Square/Copy (maximize/restore), X (close), "GS" text wordmark (logo)
- Files changed: src/renderer/src/components/Toolbar.tsx, src/renderer/src/components/TitleBar.tsx
- **Learnings for future iterations:**
  - Toolbar icons use size={18}, TitleBar icons use size={14} for consistent sizing
  - Loader2 icon is better than RefreshCw for spinner animations (has CSS animation support)
  - The force push dialog warning icon was also an emoji (⚠️) — don't forget dialog content when replacing icons
  - TitleBar logo replaced with "GS" text wordmark rather than an icon
---

## 2026-03-16 - US-042b
- Replaced all emoji/unicode icons in Sidebar.tsx with Lucide icons: ChevronRight (collapse/expand chevrons), GitBranch (branches section/tab), Globe (remotes), Tag (tags), Archive (stashes), Package (submodules), FolderOpen (files tab), ArrowRightLeft (checkout), Pencil (rename), GitMerge (merge), RotateCcw (rebase), ArrowUpFromLine (push), Trash2 (delete), X (close buttons), AlertTriangle (errors), RefreshCw (fetch), Check (checkout tag, submodule status), Circle/CircleDot (submodule status), ArrowUp/ArrowDown (ahead/behind), Download/ExternalLink (submodule actions), CornerRightUp/CornerUpRight (stash apply/pop)
- Replaced all emoji/unicode icons in StatusBar.tsx with Lucide icons: GitBranch (branch), ArrowUp/ArrowDown (ahead/behind), ArrowDownToLine (incoming), Loader2 (spinners), RefreshCw (refresh), Bell (notifications), Check/XCircle/AlertTriangle/Info (notification types)
- Changed SidebarSectionProps.icon type from `string` to `React.ReactNode` to support JSX icon elements
- Files changed: src/renderer/src/components/Sidebar.tsx, src/renderer/src/components/StatusBar.tsx
- **Learnings for future iterations:**
  - SidebarSection `icon` prop is now `React.ReactNode`, pass icons as `icon={<IconName size={16} />}`
  - Context menu icons use 14px size, section icons 16px, inline small icons 10-12px
  - `stash@&#123;...&#125;` in Sidebar.tsx are JSX escapes for curly braces, not emoji — don't replace
  - `→` in title attributes is a text arrow for tooltips, not an icon — don't replace
  - Sidebar has many nested context menu components (BranchContextMenu, RemoteContextMenu, RemoteBranchContextMenu, TagsSection, StashesSection, SubmodulesSection) — each has its own icon replacements
---
## 2026-03-17 - US-042c
- Replaced ALL emoji/unicode icons in every remaining renderer component with Lucide icons
- Files changed: NotificationToast.tsx, StatusPanel.tsx, CommitGraph.tsx, WelcomeScreen.tsx, SearchPalette.tsx, SettingsPanel.tsx, BlameView.tsx, ConflictResolver.tsx, Terminal.tsx, RebaseDialog.tsx, MergeDialog.tsx, KeyboardShortcutsPanel.tsx, BottomPanel.tsx, CommitFilterBar.tsx, RepoView.tsx, ResetDialog.tsx, DiffViewer.tsx, CodeEditor.tsx, FileTree.tsx, CloneDialog.tsx, Sidebar.tsx (missed tag icon), ContextMenu.tsx (icon prop type)
- Changed ContextMenuItem.icon type from `string` to `React.ReactNode` to support JSX Lucide icons
- Changed StatusSection.stageActionIcon type from `string` to `React.ReactNode`
- Grep for emoji/unicode patterns returns zero matches in src/renderer/src/
- **Learnings for future iterations:**
  - `→` in description strings (like "Branch → upstream") are text, not icons — leave them as-is
  - `&#123;` and `&#125;` in Sidebar.tsx are JSX-safe curly braces for stash display, not icons
  - `\u2026` (ellipsis) in BlameView is text truncation, not an icon
  - FileTree getFileIcon returns ReactNode now, not string — downstream consumers already used it in JSX so no changes needed
  - Status indicators (M, S, U, !, D, A, R) in FileTree are letter abbreviations, not emoji — keep as strings
  - ContextMenuItem.icon is now React.ReactNode — pass Lucide components with size={14}
  - `−` (U+2212 minus sign) was used in button text labels — replace with <Minus> icon
---
## 2026-03-17 - US-043a
- Created `src/renderer/src/styles/tokens.css` with design tokens: spacing (--space-xs to --space-2xl), font sizes (--font-xs to --font-xl), border radius (--radius-sm/md/lg), shadows (--shadow-sm/md/lg), transitions (--transition-fast/normal)
- Moved all color variables from global.css :root and [data-theme="light"] into tokens.css
- Both dark and light theme values defined in tokens.css via [data-theme="light"] selector
- Light theme also gets adjusted shadow values (less opacity)
- tokens.css imported before global.css in main.tsx
- global.css now starts with a comment pointing to tokens.css, then the reset/global styles
- Files changed: src/renderer/src/styles/tokens.css (new), src/renderer/src/styles/global.css, src/renderer/src/main.tsx
- **Learnings for future iterations:**
  - tokens.css is imported BEFORE global.css in main.tsx — order matters for cascading
  - Design tokens available: --space-{xs,sm,md,lg,xl,2xl}, --font-{xs,sm,md,lg,xl}, --radius-{sm,md,lg}, --shadow-{sm,md,lg}, --transition-{fast,normal}
  - When creating CSS modules, use design tokens instead of hardcoded px values
  - global.css no longer has :root or [data-theme] blocks — all in tokens.css
---
## 2026-03-17 - US-043b
- Extracted Toolbar styles from global.css into Toolbar.module.css (toolbar, buttons, progress, notifications, force-push warning, pull strategy, stash checkbox)
- Extracted TitleBar styles from global.css into TitleBar.module.css (titlebar, brand, repo display, window controls)
- Extracted StatusBar styles from global.css into StatusBar.module.css (status bar, branch info, sync, operations, notifications, history dropdown)
- Created css-modules.d.ts type declaration for TypeScript CSS module support
- Updated all three components to use scoped `styles.className` pattern
- Added missing `branch-dialog-btn-primary`, `branch-dialog-btn-secondary`, `branch-dialog-btn-danger` styles to global.css (shared across multiple components)
- Fixed broken `toolbar-spinner-rotate` keyframe reference in status bar — defined proper `spinnerRotate` keyframe in module
- Files changed: Toolbar.tsx, Toolbar.module.css (new), TitleBar.tsx, TitleBar.module.css (new), StatusBar.tsx, StatusBar.module.css (new), css-modules.d.ts (new), global.css
- **Learnings for future iterations:**
  - CSS modules need a type declaration file (`*.module.css` → `declare module`) for TypeScript
  - Shared dialog classes (branch-dialog-*) must stay in global.css, not in component modules
  - `branch-dialog-btn-primary/secondary/danger` were previously only defined in the toolbar section — they needed to be added back near the other branch-dialog styles
  - The original `toolbar-spinner-rotate` keyframe was never defined in global.css (broken reference) — fixed by defining `spinnerRotate` in StatusBar.module.css
  - When converting to CSS modules, keep global class names for shared dialogs but use module classes for component-specific styles
  - `@keyframes slideUp` is shared by multiple components — keep in global.css
---
## 2026-03-17 - US-043c
- Created `Sidebar.module.css` with all sidebar-related styles extracted from global.css
- Converted all sidebar-specific classNames in Sidebar.tsx to use `styles.xxx` scoped pattern
- Updated FileTree.tsx to import `sidebarStyles` from Sidebar.module.css for shared classes (placeholder, searchBox, searchInput, searchClear)
- Removed all sidebar-*, stash-diff-*, sidebar-submodule-* styles from global.css
- Kept branch-ctx-menu-* and branch-dialog-* styles in global.css (shared across Sidebar, FileTree, ContextMenu, MergeDialog, etc.)
- Replaced hardcoded pixel values with design tokens (--space-*, --font-*, --radius-*, --transition-*)
- Files changed: Sidebar.module.css (new), Sidebar.tsx, FileTree.tsx, global.css
- **Learnings for future iterations:**
  - FileTree.tsx uses some sidebar classes (placeholder, search-box/input/clear) — when extracting sidebar styles, also update FileTree
  - Cross-component imports of CSS modules are fine: `import sidebarStyles from './Sidebar.module.css'` works for sharing scoped styles
  - branch-ctx-menu-* and branch-dialog-* are shared across 5+ components — keep in global.css until a shared module is created
  - The `.sidebar-section-title` responsive rule in the @media query also needed removal from global.css
  - Stash items use a `selected` class conditionally — handle with separate camelCase class (stashItemSelected)
---
## 2026-03-17 - US-043d
- Created CommitGraph.module.css with all commit graph, context menu, detail panel, cherry-pick, and revert notification styles (~650 lines)
- Created StatusPanel.module.css with all status panel, status sections, file items, hunk diff viewer, and staging tab bar styles (~500 lines)
- Created DiffViewer.module.css with all diff viewer, inline/side-by-side view, word diff, binary, and truncation styles (~300 lines)
- Updated CommitGraph.tsx with styles import and lookup maps (refTypeClass, signatureClass, cherryPickStatusClass, revertStatusClass)
- Updated StatusPanel.tsx with styles import and lookup maps (sectionClassMap, iconClassMap, hunkLineTypeClass)
- Updated DiffViewer.tsx with styles import and lookup maps (lineTypeClass, sbsLineTypeClass)
- Removed ~1900 lines of extracted styles from global.css (4346 lines remaining)
- Kept syn-* syntax highlighting classes in global.css (used dynamically by highlightLine())
- Files changed: CommitGraph.module.css (new), StatusPanel.module.css (new), DiffViewer.module.css (new), CommitGraph.tsx, StatusPanel.tsx, DiffViewer.tsx, global.css
- **Learnings for future iterations:**
  - Dynamic CSS class names (e.g., `className-${type}`) need lookup maps when converting to CSS modules: `const typeClass: Record<string, string> = { added: styles.lineAdded, ... }`
  - Syntax highlighting token classes (syn-*) must stay global since they're returned as string values from pure functions
  - Cherry-pick and revert notification styles (~200 lines each) are part of CommitGraph component even though they were in separate global.css sections
  - `:global()` selector in CSS modules handles cross-module class references (e.g., `.detailDiff :global(.diff-viewer)`)
  - StatusPanel includes hunk-diff-viewer styles (~200 lines) for line-level staging functionality
  - After extracting 3 components, global.css is ~4346 lines — still has many component styles to extract
---
## 2026-03-17 - US-043e
- Extracted ALL remaining component styles from global.css into CSS modules (17 new modules)
- Components: WelcomeScreen, CloneDialog, RepoView, BottomPanel/Terminal, CommitDialog, MergeDialog, RebaseDialog, ResetDialog, ConflictResolver, CodeEditor, SearchPalette, CommitFilterBar, FileTree, BlameView, KeyboardShortcutsPanel, SettingsPanel, NotificationToast
- global.css reduced from ~4346 lines to 448 lines (8.5KB) — contains only: CSS reset, body/html, app layout, shared branch-ctx-menu/branch-dialog styles, resize handles, syntax highlighting tokens, shared slideUp animation
- Fixed orphaned class references: repo-view-spinner in CommitGraph (added local spinner class), conflict-banner/editor-toggle/code-editor-panel in RepoView (imported from respective modules), blame-view-panel in RepoView (imported BlameView module)
- Moved responsive media query from global.css to Toolbar.module.css since it only targeted toolbar classes
- WelcomeScreen agent also updated MainContent.tsx to use .main-content--centered instead of :has(.welcome-screen) selector
- Files changed: 40 files (17 new .module.css, 22 modified .tsx, global.css)
- **Learnings for future iterations:**
  - When extracting CSS modules, parent components often reference child component classes (e.g., RepoView uses blame-view-panel, conflict-banner, code-editor-panel) — import child modules in parent
  - Shared animations (@keyframes slideUp) and shared component styles (branch-ctx-menu-*, branch-dialog-*) must stay in global.css
  - `:has()` CSS selectors break when child class names become scoped — use modifier classes instead
  - After full CSS module extraction, global.css should only contain: reset, body, app layout, shared cross-component styles, syntax highlighting tokens, and shared keyframes
  - Responsive media queries referencing module-scoped classes must move to the module file
---
## 2026-03-17 - US-044a
- Restructured AppLayout into three-column layout using react-resizable-panels: left sidebar, center panel, right detail panel
- Terminal panel moved outside horizontal group to span full width below all three columns
- Layout hierarchy: Group(vertical) > [Group(horizontal) > Sidebar | Center | DetailPanel] | Terminal
- Created DetailPanel.tsx + DetailPanel.module.css — placeholder commit detail panel that shows when a commit is selected
- Updated useLayoutState to include `rightPanelSize` (default 25%) persisted to localStorage
- Wired `onCommitSelect` callback through MainContent → RepoView → CommitGraph to lift selected commit state to AppLayout
- Right panel collapses completely when no commit is selected (conditionally rendered)
- All dividers use existing resize-handle classes with visible grab handles
- Min sizes enforced via Panel minSize/maxSize props (sidebar 12-40%, center min 30%, detail 15-50%)
- Files changed: AppLayout.tsx, MainContent.tsx, RepoView.tsx, useLayoutState.ts, DetailPanel.tsx (new), DetailPanel.module.css (new)
- **Learnings for future iterations:**
  - The outer Group must be vertical to allow terminal to span full width below the horizontal column group
  - react-resizable-panels uses percentage-based sizes, not pixel sizes — min/max constraints enforce reasonable proportions
  - CommitDetail type is exported from CommitGraph.tsx — import it from there
  - The `onCommitSelect` callback chain: AppLayout → MainContent → RepoView → CommitGraph
  - DetailPanel receives the full CommitDetail object and a close handler
  - Layout state localStorage key is 'gitslop-layout-state' — new fields get defaults via spread
---
## 2026-03-17 - US-044b
- Implemented sidebar collapse/expand toggle with PanelLeftClose/PanelLeftOpen icons
- When collapsed, sidebar renders as 48px icon rail with icons for: Branches, Files, Remotes, Tags, Stashes, Submodules
- Clicking an icon in rail mode opens a floating overlay panel (280px wide) with that section's content
- Overlay closes on click outside, Escape, or close button
- Collapse state persisted via `sidebarCollapsed` in useLayoutState (localStorage)
- Collapsed sidebar rendered outside react-resizable-panels as a fixed-width flex item
- Files changed: AppLayout.tsx, Sidebar.tsx, Sidebar.module.css, useLayoutState.ts
- **Learnings for future iterations:**
  - Icon rail must be outside the react-resizable-panels Group to maintain fixed 48px width
  - Rail overlay uses `position: absolute` relative to the icon rail with `left: 48px` to float over content
  - `sidebarCollapsed` is separate from `sidebarVisible` — Ctrl+B toggles visibility, collapse button toggles rail mode
  - Both collapsed and expanded modes need their own copies of context menus and dialogs (branch context menu, new branch dialog, etc.)
  - RAIL_SECTIONS array defines icon rail entries — add new sidebar sections there when extending
---
## 2026-03-17 - US-044c
- Implemented hybrid right detail panel: inline column on wide screens (>=1400px), sliding overlay on narrow screens (<1400px)
- Created useWindowWidth hook (src/renderer/src/hooks/useWindowWidth.ts) with requestAnimationFrame-based debounce
- Updated DetailPanel to accept `overlay` prop — when true, renders with backdrop shadow and slide-in animation
- Updated AppLayout to conditionally render inline panel (in react-resizable-panels) vs overlay (fixed position)
- Overlay dismissible via: close button (X), clicking backdrop, or Escape key
- Files changed: useWindowWidth.ts (new), DetailPanel.tsx, DetailPanel.module.css, AppLayout.tsx
- **Learnings for future iterations:**
  - useWindowWidth hook uses requestAnimationFrame for performant resize tracking
  - Overlay detail panel uses `position: fixed` with `z-index: 100/101` — backdrop at 100, panel at 101
  - The `min(420px, 85vw)` width ensures overlay works on very narrow windows
  - Transition between inline and overlay is instant (React conditional rendering) — the slide-in animation only applies to overlay appearance
  - DETAIL_PANEL_BREAKPOINT (1400px) is defined as a const in AppLayout for easy adjustment
---
## 2026-03-17 - US-045
- Implemented context-aware toolbar with two modes: no-repo mode (Open, Clone, Init) and repo mode (Pull, Push, Fetch, Branch, Merge, Stash)
- Added remote detection: buttons requiring remotes (pull, push, fetch) are hidden when no remotes configured
- Disabled buttons show explanatory tooltip (e.g., "Push — No remotes configured")
- Active operations show inline spinner next to button text instead of replacing the icon
- Added overflow menu (... button) using ResizeObserver to detect when toolbar is too narrow for all buttons
- Added CloneDialog integration in toolbar for no-repo mode
- Files changed: Toolbar.tsx, Toolbar.module.css, AppLayout.tsx
- **Learnings for future iterations:**
  - `window.electronAPI.git.getRemotes()` returns array of `{ name, fetchUrl, pushUrl }` — check length for "has remotes"
  - CloneDialog accepts `onClose` and `onCloneComplete(repoPath)` props
  - Toolbar now accepts `onRepoOpen` prop for opening/cloning/initializing repos
  - ToolbarButton interface pattern is useful for building dynamic button lists with grouping
  - ResizeObserver on toolbar container detects overflow — buttonWidth ~80px per button with label
---
## 2026-03-17 - US-046
- Fixed full-screen blink/flash caused by cascading state updates on data refresh
- Root causes: (1) `setLoading(true)` on every refresh in RepoView and CommitGraph hid all content momentarily, (2) 5+ setInterval pollers (3s, 5s, 5s, 5s, 10s) triggered redundant git commands, (3) File watcher triggered multiple parallel listeners
- Fixes applied:
  - RepoView: Only shows loading spinner on initial mount, not on subsequent refreshes (uses `initialLoadDone` ref)
  - CommitGraph: Only shows loading on first load; added in-flight guard to prevent overlapping requests; diffs commits array by hash to skip no-op state updates; replaced 5s setInterval with file watcher listener
  - StatusPanel: Removed 3s setInterval polling; added in-flight guard; removed `onRefresh()` cascade from watcher (only calls it on explicit user actions); increased debounce to 500ms
  - Sidebar: Replaced ALL 5 setInterval pollers (branches, remotes, tags, stashes, submodules) with debounced onRepoChanged listeners (800ms-2000ms delays)
  - FileTree: Added 600ms debounce to watcher listener
  - StatusBar: Added 800ms debounce to watcher listener
  - MainContent: Wrapped in React.memo to prevent unnecessary re-renders from parent state changes
- Files changed: CommitGraph.tsx, FileTree.tsx, MainContent.tsx, RepoView.tsx, Sidebar.tsx, StatusBar.tsx, StatusPanel.tsx
- **Learnings for future iterations:**
  - Loading state should only be shown on initial mount, not on background refreshes — use a ref to track first load
  - setInterval polling is wasteful and causes cascading re-renders — prefer file watcher events with debouncing
  - Multiple components listening to the same onRepoChanged event should use staggered debounce delays (500ms, 600ms, 800ms, 1000ms) to avoid all firing simultaneously
  - In-flight guards (refs tracking pending async calls) prevent overlapping network requests
  - CommitGraph can diff commits by comparing array length + first/last hashes to skip no-op state updates
---
## 2026-03-17 - US-047
- Restructured sidebar into flex layout with sticky header (collapse btn + tabs) and independent scrollable content area
- Added `sidebarHeader` and `sidebarScrollArea` CSS classes for proper layout separation
- Collapse toggle button is now always pinned at top (inside flex-shrink: 0 header) and never scrolls away
- Chevron rotation animation uses 200ms ease-out for smooth expand/collapse
- Added keyboard accessibility: branch items have tabIndex={0}, Enter to checkout, arrow keys to navigate items
- Added ARIA attributes: role="region" on sections, role="listbox"/"option" on branch lists, aria-expanded on section headers
- Applied same accessibility improvements to both expanded sidebar and collapsed rail overlay branch lists
- SidebarSection now has `handleKeyDown` for ArrowDown from header to first item focus
- Added focus-visible styles for branch/remote/tag/stash/submodule items
- Files changed: Sidebar.tsx, Sidebar.module.css
- **Learnings for future iterations:**
  - Sidebar has two render paths: collapsed (icon rail) and expanded — changes to branch list need to be applied in BOTH
  - `handleListKeyDown` must be defined before the collapsed render path (early return) since it's used in both
  - Use `display: flex; flex-direction: column` + `overflow: hidden` on container with `flex: 1; overflow-y: auto` on scroll area for proper independent scrolling
  - ARIA patterns: section headers use `aria-expanded`, lists use `role="listbox"`, items use `role="option"` + `aria-selected`
---
## 2026-03-17 - US-048a
- Created `laneAssignment.ts` — pure lane assignment algorithm extracted as a standalone module
- Exports: `assignLanes()`, `parseRefs()`, `getMaxLane()`, `LANE_COLORS`, and all types (`LaneCommit`, `ParsedRef`, `ParentConnection`, `LaneAssignmentResult`)
- Algorithm walks commits (newest first), assigns lanes, handles merges, frees/reuses lanes
- HEAD commit pre-reserved in lane 0 (leftmost) via pre-scan
- 10-color Catppuccin-inspired palette, cycling for additional branches
- Second pass resolves actual parent toLane values after all commits are placed
- Created 29 unit tests covering: parseRefs, linear history, feature branches, merge commits, octopus merges, HEAD assignment, color assignment, lane reuse, root commits, getMaxLane
- Files changed: laneAssignment.ts (new), __tests__/laneAssignment.test.ts (new)
- **Learnings for future iterations:**
  - `assignLanes()` takes `LaneCommit[]` (hash, parentHashes, refs) — lighter than full GitCommit
  - CommitGraph.tsx has its own `computeGraphLayout` and `parseRefs` — US-048b should replace those with imports from laneAssignment.ts
  - The existing `GraphNode` type in CommitGraph.tsx maps closely to `LaneAssignmentResult` — can be replaced
  - Second pass to resolve parent toLane is needed because merge parents may not yet be placed when first encountered
  - LANE_COLORS is the same palette as in CommitGraph.tsx — keep in sync or replace
---
## 2026-03-17 - US-048b
- Replaced Canvas-based `GraphCanvas` with SVG-based `GraphSVG` component using `<svg>`, `<line>`, `<path>`, and `<circle>` elements
- Replaced local `computeGraphLayout()` and `parseRefs()` with imports from `laneAssignment.ts` (`assignLanes()`)
- Removed duplicate `LANE_COLORS`, `ParsedRef`, `GraphNode` definitions — now imported from laneAssignment.ts
- HEAD commit rendered with larger node (6px) + highlighted glow ring (9px, 35% opacity)
- Merge commits rendered as hollow circles (bg-primary fill, colored stroke)
- Normal commits rendered as filled colored circles (4px radius)
- Branch lines use bezier curves for merge connections (`<path>` with cubic bezier)
- SVG is React.memo'd for performance; uses `buildHashIndex` Map for O(1) parent lookups instead of findIndex
- GraphNode type now uses `lane` and `color` from laneAssignment instead of `column`
- Files changed: CommitGraph.tsx, CommitGraph.module.css
- **Learnings for future iterations:**
  - SVG overlay uses same positioning pattern as Canvas (position: absolute, pointerEvents: none, z-index: 1)
  - `assignLanes()` returns `LaneCommit` not `GitCommit` — map back to full commit data after calling
  - Build a `Map<hash, index>` for O(1) parent lookups instead of repeated `findIndex`
  - `node.color` from lane assignment is pre-computed — no need to index into LANE_COLORS manually
  - React.memo on GraphSVG prevents unnecessary re-renders when parent state changes
  - SVG `overflow: visible` ensures lines extending beyond viewport boundaries render correctly
---
## 2026-03-17 - US-048c
- Implemented Canvas-based fallback renderer (`GraphCanvas`) for repos with 50,000+ commits
- Canvas renderer produces visually identical output to SVG: same colors, node sizes, bezier curves, HEAD glow rings, merge hollow nodes
- Uses same `assignLanes()` from laneAssignment.ts and same `buildHashIndex()` for O(1) parent lookups
- Automatic switch via `CANVAS_THRESHOLD = 50000` const — `useCanvas = nodes.length >= CANVAS_THRESHOLD`
- Canvas renders only visible viewport + buffer (renderStart/renderStop), same as SVG
- High-DPI support via `devicePixelRatio` scaling
- Hit-testing for click/hover: distance-based coordinate mapping with generous hit radii
- `onNodeHover` changes cursor to pointer, `onNodeClick` delegates to commit selection logic
- Files changed: CommitGraph.tsx
- **Learnings for future iterations:**
  - Canvas needs explicit `devicePixelRatio` scaling: set canvas.width = width * dpr, then ctx.scale(dpr, dpr)
  - Canvas `pointerEvents: 'auto'` (not 'none' like SVG) since it handles its own mouse events
  - Hit-testing uses reverse iteration so top-most nodes win on overlap
  - Canvas merge node fill uses hardcoded '#1e1e2e' instead of CSS var — consider reading computed style if theme switching is needed
  - The `canvasHoverIndex` state drives cursor change on the viewport container
---
## 2026-03-17 - US-049
- Added comprehensive commit graph interaction features
- Added "Checkout" action to commit context menu (calls git.checkout with commit hash)
- Added BranchRefContextMenu component for right-clicking branch/tag ref labels: checkout, merge into current, rebase onto, delete, push, copy name
- Added hover tooltip showing full commit message preview (appears after 600ms delay, max 300 chars, styled popover)
- Added Enter key to open commit details for selected commit in keyboard navigation
- Context menus now close on scroll (added scroll listener with capture)
- Context menu repositions to stay within viewport bounds
- Added tooltip CSS styles to CommitGraph.module.css
- Files changed: CommitGraph.tsx, CommitGraph.module.css
- **Learnings for future iterations:**
  - `git.deleteBranch()` takes `opts?: { force?: boolean }` not a raw boolean — use `{ force: false }`
  - `git.push()` options don't include `tags` — use `git.push(repoPath, {})` for basic push
  - Tooltip timer refs need cleanup on unmount to avoid memory leaks
  - `window.addEventListener('scroll', handler, true)` with `true` captures scroll events from child elements (needed for react-window scroll)
  - BranchRefContextMenu needs the `repoPath` prop directly since it calls git APIs
---
## 2026-03-17 - US-050
- Implemented full Commit Detail Panel with rich file information and inline diff viewing
- Enhanced `git-service.ts` `showCommit()` to return file statuses (A/D/M/R/C), insertions/deletions per file, and total stats using `--name-status` and `--numstat`
- Added `CommitFileDetail` type to CommitGraph.tsx with path, status, insertions, deletions, oldPath fields
- Redesigned DetailPanel.tsx with: copyable SHA (click + copy button with success feedback), absolute + relative date display, Lucide status icons per file (FilePlus/FileMinus/FileEdit/FileSymlink), file count summary with +insertions/-deletions, collapsible file list
- Click a file to load and display its diff inline using the existing DiffViewer component via `showCommitFileDiff` IPC
- Added `repoPath` prop to DetailPanel for IPC calls; updated AppLayout to pass it
- Files changed: git-service.ts, CommitGraph.tsx, DetailPanel.tsx, DetailPanel.module.css, AppLayout.tsx
- **Learnings for future iterations:**
  - `showCommit` now returns `fileDetails`, `totalInsertions`, `totalDeletions` in addition to `files` — backward compatible
  - `--name-status` returns status codes: M (modified), A (added), D (deleted), R (renamed), C (copied)
  - For renames, `--name-status` outputs `R100\told.txt\tnew.txt` — need to parse tab-separated fields
  - DetailPanel needs `repoPath` to call `showCommitFileDiff` — added as required prop
  - DiffViewer accepts `className` prop for custom styling
  - Reset selectedFile when commit.hash changes to avoid stale diff display
---
## 2026-03-17 - US-051
- Enhanced DetailPanel with integrated diff viewer features for commit file diffs
- Added inline/side-by-side mode toggle (Inline/Split buttons) in diff header
- Added diff header showing: file path, change type label (Added/Modified/Deleted/Renamed), old path for renames
- Binary files show "Binary file changed" message with Package icon
- Large diffs (1000+ lines) collapsed by default with "Expand" button
- Added prev/next file navigation buttons with file counter (1/N) and [ / ] keyboard shortcuts
- Files changed: DetailPanel.tsx, DetailPanel.module.css
- **Learnings for future iterations:**
  - DiffViewer exports `DiffViewMode` type — import it for mode state typing
  - Binary file detection: check for 'Binary files' or 'GIT binary patch' in diff content
  - `handleFileClick` is a `useCallback` — keyboard navigation effect that calls it must list it in deps
  - `initialMode` prop on DiffViewer sets the starting mode — pass the current mode state for mode sync
  - File navigation uses `findIndex` on `fileDetails` array to determine current position
---
## 2026-03-17 - US-052
- Redesigned StatusPanel from vertical three-section layout (Staged/Unstaged/Untracked) to two-column layout (Unstaged left, Staged right)
- Integrated commit form (subject, body, amend, sign-off, GPG, commit & push) into the bottom of the staged column, replacing the separate CommitDialog component
- Made the entire staging area collapsible with a chevron toggle header showing file count badge
- Merged unstaged + untracked files into a single "Unstaged Changes" column on the left
- Added discard button (Trash2 icon) per file in the unstaged column alongside the stage (+) button
- Added "Copy Path" to the file context menu
- Repositioned StatusPanel below CommitGraph in RepoView (staging area at bottom of center panel)
- Removed CommitDialog import from RepoView since commit form is now integrated into StatusPanel
- Kept all existing functionality: drag-and-drop, hunk staging, context menus, keyboard shortcuts, diff viewer with staging/enhanced tabs
- Files changed: StatusPanel.tsx (rewritten), StatusPanel.module.css (rewritten), RepoView.tsx (moved StatusPanel below graph, removed CommitDialog)
- **Learnings for future iterations:**
  - StatusPanel now imports from useSettings and useKeyboardShortcuts hooks for commit functionality
  - The commit form Ctrl+Enter shortcut is scoped to the panel via panelRef.current.contains(active)
  - Two-column layout uses `display: flex` with `flex: 1` on each column and a border between them
  - CommitDialog.tsx is no longer used by RepoView but still exists — could be removed in a cleanup pass
  - The `_forStaging` parameter in buildLinesPatch was prefixed with underscore since it's unused (lint compliance)
---
## 2026-03-17 - US-053
- Redesigned WelcomeScreen with polished layout and new features
- Added keyboard shortcut hints (kbd elements) on action cards: Ctrl+O (Open), Ctrl+Shift+C (Clone), Ctrl+Shift+I (Init)
- Added "No recent repositories" empty state message when recent repos list is empty (previously section was hidden entirely)
- Recent repos section now always visible with either list or empty message
- Added CSS for shortcut badges (.welcomeActionShortcut) with subtle border/background, opacity animation on hover
- Added CSS for empty state (.welcomeRecentEmpty) with italic muted styling
- Files changed: WelcomeScreen.tsx, WelcomeScreen.module.css
- **Learnings for future iterations:**
  - WelcomeScreen already had most features from previous iterations (Lucide icons, action cards, recent repos with remove button)
  - `<kbd>` element is semantically correct for keyboard shortcut hints
  - The existing toolbar uses Ctrl+O for Open Repository — keep shortcut hints consistent with toolbar tooltips
---
## 2026-03-17 - US-054
- Added smooth 200ms ease-out animations for all panel collapse/expand transitions
- Sidebar: fadeIn animation on expanded sidebar, railSlideIn animation on icon rail, railOverlaySlideIn on floating overlay
- Detail panel: fadeIn animation on inline mode (overlay already had slideInFromRight)
- Terminal panel: fadeIn animation on bottom panel content, slideInFromBottom animation class on panel wrapper
- Added panelContentFadeIn, slideInFromLeft, slideInFromBottom keyframes to global.css
- Added panel animation classes (panel-animate-sidebar, panel-animate-detail, panel-animate-terminal) applied via react-resizable-panels className prop
- Implemented double-click on dividers to reset panels to default sizes using usePanelRef imperative API (resize method)
- Added cursor: col-resize / row-resize to resize handles for better UX
- Files changed: AppLayout.tsx, BottomPanel.module.css, DetailPanel.module.css, Sidebar.module.css, global.css
- **Learnings for future iterations:**
  - react-resizable-panels v4 supports `panelRef` with imperative `resize()`, `collapse()`, `expand()` methods
  - `usePanelRef()` hook from react-resizable-panels creates a ref that can be passed to Panel's `panelRef` prop
  - Separator accepts `...rest` HTML props including `onDoubleClick` for double-click-to-reset
  - CSS animations on panel wrappers (via Panel `className`) trigger on mount, providing smooth expand transitions
  - Since panels are conditionally rendered with `&&`, mount-time @keyframes animations effectively animate expand
  - For collapse animations (unmount), React doesn't support animation-before-unmount natively — use mount animations only for visual polish
---
## 2026-03-17 - US-055a
- Enhanced ContextMenu component with CSS modules, scroll-close, and auto-separator before danger items
- Created ContextMenu.module.css with scoped styles using design tokens (--radius-md, --shadow-lg, --space-*, --font-*, --transition-fast)
- Added menuFadeIn animation (120ms ease-out scale + opacity) for polished appearance
- Added focus-visible outline style for keyboard accessibility
- Added scroll listener (capture phase) to close menu on any scroll event
- Added auto-separator insertion before danger items via useMemo preprocessing of items array
- Kept branch-ctx-menu-* styles in global.css — Sidebar.tsx and FileTree.tsx still use them directly (US-055b will migrate)
- Files changed: ContextMenu.tsx (rewritten with CSS modules), ContextMenu.module.css (new)
- **Learnings for future iterations:**
  - `composes: item` in CSS modules lets itemDanger inherit base item styles without duplication
  - Auto-separator before danger items uses a synthetic key `__danger-sep-${entry.key}` to avoid key collisions
  - Scroll close uses `window.addEventListener('scroll', handler, true)` — the capture `true` is critical to catch scrolls from inner elements
  - The ContextMenu component interface (props) is unchanged — existing consumers (StatusPanel) work without modification
  - branch-ctx-menu-* in global.css is still needed by Sidebar.tsx and FileTree.tsx which build context menus with raw HTML instead of the ContextMenu component
---
## 2026-03-17 - US-055b
- Replaced ALL custom context menu implementations with the reusable ContextMenu component from US-055a
- CommitGraph.tsx: Replaced CommitContextMenu and BranchRefContextMenu components with builder functions (buildCommitContextMenuItems, buildBranchRefContextMenuItems) that return ContextMenuEntry[]
- Sidebar.tsx: Replaced BranchContextMenu, RemoteContextMenu, RemoteBranchContextMenu components with builder functions; replaced inline tag/stash/submodule context menus with ContextMenu component
- FileTree.tsx: Replaced FileTreeContextMenu component with buildFileTreeContextMenuItems builder function
- Removed ~570 lines of duplicated context menu code (click-outside handlers, escape key handlers, scroll handlers, menu rendering)
- Removed unused branch-ctx-menu-* CSS from global.css (73 lines) and ctxMenu* CSS from CommitGraph.module.css (60 lines)
- StatusPanel already used ContextMenu component — no changes needed there
- Files changed: CommitGraph.tsx, CommitGraph.module.css, Sidebar.tsx, FileTree.tsx, global.css
- **Learnings for future iterations:**
  - Builder function pattern (returning ContextMenuEntry[]) is cleaner than wrapper components for context menus — the ContextMenu component handles all the dismiss/position logic
  - When removing old context menu components, also remove their manual close-on-click-outside useEffect hooks since ContextMenu handles this internally
  - Removing branch-ctx-menu-* from global.css is safe once all TSX files use the ContextMenu component — grep to verify zero references before deleting
  - The `danger: true` property on ContextMenuEntry auto-inserts a separator before danger items — no need to add explicit separators before destructive actions
  - Submodule context menu items are conditional on submodule status — use IIFE pattern `(() => { ... })()` to build items inline
---
## 2026-03-17 - US-056
- Created reusable Skeleton component system: Skeleton (base), SkeletonRow, SkeletonList, SidebarSkeleton, CommitGraphSkeleton, DiffSkeleton, RepoViewSkeleton
- Replaced loading spinner in RepoView with RepoViewSkeleton (summary cards + graph skeleton)
- Replaced loading spinner in CommitGraph with CommitGraphSkeleton (header + 12 commit rows with lanes)
- Replaced loading diff spinner in DetailPanel with DiffSkeleton (header + 8 code lines)
- Replaced "Loading branches..." text in Sidebar with SkeletonList (both collapsed rail and expanded paths)
- Removed unused Loader2 import from RepoView and DetailPanel
- Skeleton uses CSS pulse animation (1.5s ease-in-out) with --text-secondary at varying opacity
- StatusBar already had progress display for long operations (percentage + operation name)
- Files changed: Skeleton.tsx (new), Skeleton.module.css (new), RepoView.tsx, CommitGraph.tsx, DetailPanel.tsx, Sidebar.tsx
- **Learnings for future iterations:**
  - Skeleton components use `aria-hidden="true"` for accessibility — screen readers skip them
  - VARIED_WIDTHS array provides natural-looking varied skeleton row widths
  - Skeleton pulse animation uses opacity 0.15-0.4 range on --text-secondary for subtle effect
  - Skeleton components are composable: SidebarSkeleton uses SkeletonList, RepoViewSkeleton uses CommitGraphSkeleton
  - StatusBar already implements operation progress with percentage — no additional work needed for that criterion
---
## 2026-03-17 - US-057
- Implemented responsive layout for small windows with three breakpoint tiers
- < 900px: Sidebar auto-collapses to icon rail mode (auto-restores when window widens if auto-collapsed)
- < 700px: Toolbar buttons collapse to icon-only (CSS media query already existed), overflow detection uses 36px per icon-only button; TitleBar hides repo path and constrains name width; StatusBar hides center section and sync indicators
- Minimum window size: Changed from 800x600 to 800x500 in Electron main process
- Added `setSidebarCollapsed(boolean)` to useLayoutState hook for programmatic sidebar collapse
- Added `autoCollapsedRef` pattern to AppLayout to track whether sidebar was auto-collapsed vs user-collapsed
- Text truncation with ellipsis added to StatusBar branch name (max-width 200px, 120px at <700px)
- TitleBar repo path hidden at 900px (was previously 700px), repo name constrained at 700px
- Detail panel overlay at <1200px already covered by existing 1400px DETAIL_PANEL_BREAKPOINT
- Panel min-width constraints already enforced by react-resizable-panels minSize props
- Files changed: AppLayout.tsx, Toolbar.tsx, StatusBar.module.css, TitleBar.module.css, useLayoutState.ts, src/main/index.ts
- **Learnings for future iterations:**
  - Use `autoCollapsedRef` pattern to distinguish between user-initiated and responsive auto-collapse — only auto-restore if auto-collapsed
  - Toolbar overflow calculation needs to account for icon-only mode at < 700px (36px vs 80px per button)
  - StatusBar `.branch` is a flex item — needs `overflow: hidden` + `text-overflow: ellipsis` + `max-width` for proper truncation
  - Electron `minHeight`/`minWidth` in BrowserWindow config enforces minimum window size at OS level
  - The DETAIL_PANEL_BREAKPOINT (1400px) already covers the <1200px overlay criterion — no additional breakpoint needed
---
## 2026-03-17 - US-058
- Redesigned notification toast system with slide-in/fade-out animations and max 3 visible toasts
- Changed toast entrance from translateY to translateX(100%) slide-in from right (0.25s ease-out)
- Added exit animation: toast gets `exiting` flag → CSS transition fades opacity + slides right over 300ms → removed from DOM after animation
- Limited visible toasts to 3 (MAX_VISIBLE_TOASTS const) — older toasts hidden, newest shown
- Changed history limit from 100 to 50 (MAX_HISTORY const)
- Warning notifications now auto-dismiss at 8s (same as errors), up from 4s
- Notification bell in status bar already had unread count badge — no changes needed
- History dropdown already existed with proper styling — no changes needed
- Files changed: useNotifications.ts, NotificationToast.tsx, NotificationToast.module.css
- **Learnings for future iterations:**
  - Exit animations in React require a two-phase approach: set `exiting` flag → CSS transition → setTimeout removal
  - The 300ms removal timeout must match the CSS transition duration for smooth exit
  - `notifications.slice(-MAX_VISIBLE_TOASTS)` shows the most recent N toasts (newest at bottom)
  - useNotifications returns pre-sliced `visibleNotifications` — consumers don't need to worry about limiting
---

## 2026-03-17 - US-059
- Redesigned TitleBar with three-section layout: left (GitSlop wordmark), center (repo name + branch with click-to-copy), right (theme toggle + window controls)
- Redesigned StatusBar with: left (branch + ahead/behind), center (last operation status with relative time), right (encoding/line ending indicators + refresh + notification bell)
- Status bar text is muted by default and becomes visible on hover
- Added relative time formatting for last completed operation (e.g., "Pushed 2m ago")
- Files changed: TitleBar.tsx, TitleBar.module.css, StatusBar.tsx, StatusBar.module.css
- **Learnings for future iterations:**
  - TitleBar fetches its own branch info via `window.electronAPI.git.getCurrentBranch` — self-contained, no need to thread branch through App.tsx
  - `-webkit-app-region: drag` on parent, `-webkit-app-region: no-drag` on interactive children is the Electron pattern for custom titlebars
  - StatusBar tracks last completed operation by saving activeOp state when it clears after timeout
---

## 2026-03-17 - US-060
- Created ErrorBoundary React class component with componentDidCatch
- ErrorBoundary wraps AppLayout in App.tsx (TitleBar stays outside so window controls work during crashes)
- Error screen shows: AlertOctagon icon, error message, collapsible stack trace, Reload App button
- Added window.onerror and window.onunhandledrejection handlers in main.tsx
- Files changed: ErrorBoundary.tsx, ErrorBoundary.module.css (new), App.tsx, main.tsx
- **Learnings for future iterations:**
  - ErrorBoundary must be a class component (React doesn't support error boundaries as function components)
  - TitleBar is kept outside ErrorBoundary so users can still close/minimize the window during crashes
  - Global error handlers (window.onerror, onunhandledrejection) catch non-React errors like async failures
---

## 2026-03-17 - US-061
- Fixed sidebar collapse/expand to ensure sidebar is always accessible
- Three root causes fixed:
  1. Auto-collapse useEffect in AppLayout.tsx re-triggered on sidebarCollapsed state changes, overriding manual user expansion at narrow widths. Changed to only trigger on window width threshold crossing using prevWidthNarrowRef.
  2. Ctrl+B (toggleSidebar in useLayoutState.ts) toggled sidebarVisible which hid the icon rail entirely. Changed to cycle: expanded → collapsed (icon rail) → expanded.
  3. toggleSidebarCollapse and setSidebarCollapsed now always set sidebarVisible=true so icon rail can never be hidden.
- Files changed: `src/renderer/src/hooks/useLayoutState.ts`, `src/renderer/src/components/AppLayout.tsx`
- **Learnings for future iterations:**
  - The auto-collapse responsive logic must only react to window width threshold *crossing*, not to every state change, to avoid fighting user's manual toggles
  - `sidebarVisible` controls whether sidebar renders at all (including icon rail); `sidebarCollapsed` controls whether it's icon rail vs full sidebar
  - Always ensure sidebarVisible=true when changing collapse state to prevent the sidebar from becoming completely hidden
---

## 2026-03-17 13:39 - US-062
- Fixed commit graph scroll rendering by deriving scrollOffset from visibleRange instead of tracking it separately
- The viewport div has overflow:hidden and never scrolls — the react-window List manages its own scroll internally
- Replaced `useState(scrollOffset)` + `onScroll` handler with computed `visibleRange.start * ROW_HEIGHT`
- Increased SVG/Canvas render buffer from 5/10 to 15 rows above/below visible range
- Files changed: `src/renderer/src/components/CommitGraph.tsx`
- **Learnings for future iterations:**
  - The viewport div has `overflow: hidden` — only react-window's internal List scrolls. Never rely on viewport div's scrollTop for positioning.
  - scrollOffset must be derived from react-window's visibleRange to stay in sync
  - Generous render buffers (15+ rows) prevent gaps during fast scrolling
---

## 2026-03-17 - US-063
- Added `diffError` state to DetailPanel to track and display IPC errors
- When `showCommitFileDiff` IPC returns an error, error message is now displayed with AlertCircle icon in red
- When IPC throws an exception, "Failed to load diff: <message>" is shown
- When diff content is empty (no changes), shows "No changes in this file" instead of "No diff available"
- Added `.diffError` CSS class in DetailPanel.module.css with red styling and error background
- CodeEditor.tsx already handles errors properly — no changes needed there
- Files changed: `src/renderer/src/components/DetailPanel.tsx`, `src/renderer/src/components/DetailPanel.module.css`
- **Learnings for future iterations:**
  - DetailPanel error flow: `diffError` state is separate from `diffContent` — check error first in render, then binary, then content, then empty
  - CodeEditor already has proper error handling with `setError` state — use as reference pattern
---

## 2026-03-17 13:50 - US-064
- Fixed file watcher infinite loop that caused app crashes
- Files changed: `src/main/index.ts`, `src/main/git-ipc.ts`
- Three-pronged fix:
  1. Watcher now ignores ALL `.git/` directory changes (previously allowed `.git/refs` through)
  2. Increased debounce from 300ms to 500ms
  3. Added operation-level suppression: `gitOperationStarted()`/`gitOperationFinished()` wraps all mutating git IPC handlers, suppressing watcher events during operations and for 1s after the last operation completes
- Also ignores `node_modules/` in watcher
- **Learnings for future iterations:**
  - `src/main/index.ts` exports `suppressWatcher()`, `gitOperationStarted()`, `gitOperationFinished()` — use these from git-ipc.ts via `withWatcherSuppression()` helper
  - All mutating git IPC handlers are wrapped with `withWatcherSuppression()` to prevent cascading refreshes
  - The watcher double-checks suppression state both at event time and at debounced send time
  - `awaitWriteFinish.stabilityThreshold` is 300ms — files must be stable for 300ms before triggering events
---

## 2026-03-17 - US-065a
- Implemented multi-repo tab bar with state management
- Files changed: App.tsx (refactored to use tabs), new TabBar.tsx + TabBar.module.css, new useRepoTabs.ts hook
- TabBar renders below titlebar, shows only when 2+ tabs are open
- Each tab shows repo name with GitBranch icon and close (X) button
- Active tab highlighted with accent underline
- Middle-click to close tabs
- Tab state persists in localStorage (gitslop-repo-tabs key)
- Opening a repo adds a new tab or switches to existing
- Closing last tab returns to welcome screen
- Ctrl+Tab / Ctrl+Shift+Tab keyboard shortcuts for cycling tabs
- **Learnings for future iterations:**
  - useRepoTabs hook uses localStorage similar to useLayoutState pattern
  - TabBar returns null when <=1 tab (no visual change for single-repo users)
  - Tab state: { tabs: RepoTab[], activeIndex: number } with debounced persistence
  - openTab() deduplicates by repoPath — if already open, just switches to it
  - App.tsx now delegates to useRepoTabs instead of useState<string | null>
---

## 2026-03-17 14:00 - US-065b
- Implemented per-tab state isolation for multi-repo tabs
- Added `TabPerTabState` interface and `saveTabState`/`getTabState` to useRepoTabs hook
- Per-tab state stored in localStorage (`gitslop-tab-states`) as a Record<repoPath, TabPerTabState>
- AppLayout saves selectedCommit, sidebarCollapsed, detailPanelOpen when switching tabs and restores on tab switch
- Added drag-and-drop tab reordering to TabBar with visual drop indicators
- Added `reorderTabs` function to useRepoTabs hook with correct activeIndex tracking
- File watcher already correctly switches via RepoView useEffect cleanup
- Files changed: useRepoTabs.ts, TabBar.tsx, TabBar.module.css, AppLayout.tsx, App.tsx
- **Learnings for future iterations:**
  - `useRef` does NOT support lazy initializers like `useState` — must call the function: `useRef(loadFn())` not `useRef(loadFn)`
  - Per-tab state uses `useRef` (not `useState`) to avoid re-renders on every state save — only the active tab's state drives UI
  - File watcher already switches correctly because RepoView's useEffect cleanup calls `watcher.stop()` and the new mount calls `watcher.start(newRepoPath)`
  - Tab reordering with drag-and-drop: use native HTML5 drag events, track drag position relative to tab midpoint for left/right indicators
---

## 2026-03-17 14:10 - US-066
- Implemented comprehensive stability test suite
- Created ErrorBoundary tests (6 tests): catches render errors, displays fallback UI, toggles stack trace, reload button works, logs errors
- Created watcher-utils tests (17 tests): shouldIgnorePath for .git/node_modules filtering, suppression state machine, debounced event coalescing
- Created useRepoTabs tests (23 tests): open/close/switch/reorder tabs, per-tab state isolation, localStorage persistence and restoration
- Verified existing lane assignment tests (56 tests) are comprehensive
- Extracted watcher logic from src/main/index.ts into src/main/watcher-utils.ts for testability
- Files changed: src/main/watcher-utils.ts (new), src/main/index.ts (refactored), src/main/__tests__/watcher-utils.test.ts (new), src/renderer/src/components/__tests__/ErrorBoundary.test.tsx (new), src/renderer/src/hooks/__tests__/useRepoTabs.test.ts (new), package.json
- **Learnings for future iterations:**
  - vitest v4 with forks pool has issues with vi.useFakeTimers() in afterEach hooks — use real timers with short delays for debounce tests
  - jsdom has ESM compatibility issues with vitest v4 — use happy-dom instead (`@vitest-environment happy-dom` comment)
  - @testing-library/react requires @testing-library/dom as a peer dependency — install both
  - Use `cleanup()` from @testing-library/react in afterEach for happy-dom environment
  - No vitest.config.ts needed — tests work without it using electron-vite defaults; per-file `@vitest-environment` comments work for setting jsdom/happy-dom
---

## 2026-03-17 23:10 - US-GT-001a (GUI Test Suite branch: ralph/gui-tests)
- Created test framework core: framework.py with TestResult, GUITest, TestSuite classes
- Created run.py entry point with build, launch, wait, kill, and report steps
- Created dummy test (test_dummy.py) that takes a screenshot and passes
- Created directory structure: scripts/gui-tests/{tests,results,baselines}
- Added results/ to .gitignore (except .gitkeep)
- Used file-based imports (importlib.util.spec_from_file_location) to handle hyphenated package name
- Files changed: scripts/gui-tests/{__init__.py, framework.py, run.py, tests/__init__.py, tests/test_dummy.py, results/.gitkeep, baselines/.gitkeep}, .gitignore
- **Learnings for future iterations:**
  - Package name `gui-tests` has a hyphen — can't use normal Python imports. Use importlib.util.spec_from_file_location for all cross-module imports.
  - Runner supports --no-build and --no-launch flags for testing without Electron
  - Screenshots go to scripts/gui-tests/results/screenshots/{TestName}_{name}.png
  - Report is written to scripts/gui-tests/results/report.json
  - The display is :1 for X11 operations
---

## 2026-03-17 - US-GT-001b
- Added screen interaction helpers to GUITest base class in framework.py
- Methods added: click(), rightclick(), doubleclick(), type_text(), press_key(), drag(), focus_window(), _to_abs()
- All click/drag methods translate window-relative to absolute screen coords via _to_abs()
- Created tests/test_settings_click.py — clicks the Settings gear icon and takes before/after screenshots
- Imported Xlib.ext.xtest.fake_input and XK directly in framework.py (no dependency on screen-control.py)
- Files changed: scripts/gui-tests/framework.py, scripts/gui-tests/tests/test_settings_click.py
- **Learnings for future iterations:**
  - The screen interaction code from screen-control.py was reimplemented directly in framework.py using python-xlib — no import of screen-control.py needed
  - focus_window() uses _NET_ACTIVE_WINDOW ClientMessage to raise the window via the window manager
  - All coordinate helpers use get_window_bounds() which returns (abs_x, abs_y, width, height)
  - The Settings gear icon is approximately at (window_width - 30, 45) in window-relative coordinates
---

## 2026-03-17 - US-GT-001c
- Added `resize_window(width, height)` and `reset_window_size()` methods to GUITest base class in framework.py
- Wired up the `--size WxH` runner flag in run.py to resize window after launch
- Created test_resize.py that resizes to 800x500, verifies bounds, restores to 1280x800
- Files changed: scripts/gui-tests/framework.py, scripts/gui-tests/run.py, scripts/gui-tests/tests/test_resize.py
- **Learnings for future iterations:**
  - X11 `win.configure(width=w, height=h)` + `d.sync()` is the way to resize windows via python-xlib
  - After resize, always re-read bounds with `get_window_bounds()` as window position may change
  - Allow ~500ms after resize for window manager processing and UI reflow
---

## 2026-03-17 23:25 - US-GT-002
- Created `scripts/gui-tests/evaluator.py` with `evaluate()`, `save_baseline()`, `evaluation_to_dict()`, and `ScreenshotEvaluation` dataclass
- Added `assert_visual(name, criteria)` method to `GUITest` base class in `framework.py`
- Added `_evaluations` list to `GUITest` and `evaluations` field to `TestResult` dataclass
- Updated `run.py` report JSON to include evaluations per test
- Baselines saved to `scripts/gui-tests/baselines/{test_name}/` on first run (existing preserved)
- **Files changed:** `evaluator.py` (new), `framework.py`, `run.py`
- **Learnings for future iterations:**
  - `_get_evaluator()` lazy-imports evaluator.py to avoid circular imports — use same pattern for other framework extensions
  - `assert_visual()` both takes screenshot AND stores evaluation — use it instead of bare `screenshot()` when criteria matter
  - Baselines are NOT overwritten on subsequent runs — delete manually to update
---

## 2026-03-17 - US-GT-003
- Created `scripts/gui-tests/fixtures.py` with `create_test_repo()`, `cleanup_test_repo()`, and `open_test_repo()` helpers
- Created `scripts/gui-tests/tests/test_fixtures.py` with comprehensive verification test
- Test repo has: 5 main commits, feature branch with 3 commits merged back, 2 tags (v1.0, v2.0), 1 stash, staged/unstaged/untracked files
- All commits use deterministic dates (GIT_AUTHOR_DATE/GIT_COMMITTER_DATE) and author info
- Verified all state programmatically (branches, tags, stash, staged/unstaged/untracked files)
- Files changed: `scripts/gui-tests/fixtures.py`, `scripts/gui-tests/tests/test_fixtures.py`
- **Learnings for future iterations:**
  - Import pattern for test files: use `importlib.util.spec_from_file_location` to import from `gui-tests` (hyphen in name)
  - `create_test_repo()` returns temp dir path; always call `cleanup_test_repo()` in `tearDown()`
  - `open_test_repo()` uses Ctrl+O to open file dialog - may need adjustment depending on app's actual open-repo flow
  - Git stash requires files to be staged first (`git add` then `git stash push`)
---

## 2026-03-17 - US-GT-004
- Added `gui-test` npm script to package.json: `python3 scripts/gui-tests/run.py`
- Verified all directory structure already existed from prior stories (results/.gitkeep, baselines/.gitkeep, tests/__init__.py, .gitignore rules)
- Added `__pycache__/` to .gitignore to clean up untracked pycache directories
- Files changed: package.json, .gitignore
- **Learnings for future iterations:**
  - Most directory structure was already created by prior stories (US-GT-001a through US-GT-003) — this story was mostly about the npm script integration
  - `__pycache__` dirs were showing in git status — always add Python cache dirs to .gitignore early
---

## 2026-03-17 - US-GT-005
- Implemented TestWelcomeScreen GUI test class
- Files changed: scripts/gui-tests/tests/test_welcome.py (new)
- Test verifies: welcome screen dark theme, toolbar (Open/Clone/Init only), status bar ("No repository open"), theme toggle to light and back
- Uses assert_visual() with descriptive criteria for AI evaluation
- Screenshots: welcome_dark, welcome_toolbar, welcome_statusbar, welcome_light, welcome_dark_restored
- **Learnings for future iterations:**
  - Theme toggle button is approximately at (w-90, 15) relative to window — top-right area near window controls
  - Tests follow import pattern: importlib.util to load framework.py (package has hyphen in name)
  - assert_visual() combines screenshot + evaluator + baseline saving in one call
  - Welcome screen tests run on app launch before any repo is opened
---

## 2026-03-17 - US-GT-006
- Implemented test for Toolbar and Titlebar verification after opening a repo
- Files changed: scripts/gui-tests/tests/test_toolbar_titlebar.py (new)
- Test creates a test repo via fixtures, opens it, then verifies:
  - Toolbar shows Pull, Push, Fetch, Branch, Merge, Stash with Lucide icons
  - Settings gear visible on far right
  - Titlebar layout: wordmark left, repo+branch center, controls right
  - Fetch button spinner during operation
  - Click center titlebar for 'Copied!' feedback
- **Learnings for future iterations:**
  - Toolbar buttons are approximately at y=65 (below ~30px titlebar)
  - Titlebar center clickable area is at y=15
  - Fetch button is roughly 3rd button in toolbar at x~320
  - Test repo fixtures provide open_test_repo() helper for loading repos
---

## 2026-03-17 - US-GT-007
- Created scripts/gui-tests/tests/test_statusbar.py with class TestStatusBar(GUITest)
- Test opens test repo, verifies status bar elements: branch name with GitBranch icon, UTF-8/LF indicators, refresh button, notification bell
- Tests hover effect on status bar using drag for mouse positioning (no move_mouse method)
- Tests notification bell click opens history dropdown
- Tests Escape closes the dropdown
- Files changed: scripts/gui-tests/tests/test_statusbar.py
- **Learnings for future iterations:**
  - No `move_mouse` method in framework — use `drag(x-1, y, x, y)` for hover simulation
  - Status bar is 24px tall at bottom; use `h - 12` for vertical center
  - Notification bell is approximately 30px from right edge of window
---

## 2026-03-17 - US-GT-008
- Implemented TestSidebar test class in scripts/gui-tests/tests/test_sidebar.py
- Test opens a test repo, ensures sidebar is expanded via Ctrl+B toggle
- Verifies Branches tab with GitBranch Lucide icons and highlighted current branch
- Tests section header collapse/expand by clicking chevron
- Takes 4 assert_visual screenshots: expanded sidebar, branches tab, collapsed section, expanded section
- Files changed: scripts/gui-tests/tests/test_sidebar.py (new)
- **Learnings for future iterations:**
  - Use double Ctrl+B toggle to guarantee expanded state (first toggle may collapse if already expanded)
  - Sidebar tab buttons are approximately at y=95, section headers around y=125
  - Follow the established import pattern with importlib.util for framework and fixtures
---

## 2026-03-17 - US-GT-009
- Implemented Test — Sidebar Icon Rail Collapse
- Files changed: `scripts/gui-tests/tests/test_sidebar_collapse.py` (new)
- Test verifies: sidebar collapse to 48px icon rail, floating overlay panel opens on icon click, overlay closes on outside click, expand button restores full sidebar
- **Learnings for future iterations:**
  - Collapse button is at approximately (230, 73) — near right edge of sidebar header
  - Icon rail center is at x=24 (center of 48px rail)
  - Section icons in the rail start at approximately y=120
  - Use Ctrl+B toggle twice to guarantee expanded sidebar state before testing collapse
---

## 2026-03-17 - US-GT-010
- Implemented TestLayout test class in scripts/gui-tests/tests/test_layout.py
- Test verifies: two-column layout (sidebar + center), three-column layout (clicking commit opens detail panel), panel divider drag resize with before/after screenshots, terminal toggle via Ctrl+`, and detail panel close via X button
- Files changed: scripts/gui-tests/tests/test_layout.py (new)
- **Learnings for future iterations:**
  - Commit graph rows are approximately at y=160 (below toolbar) with x around 500 for center
  - Detail panel close button is near top-right of the window (w-20, ~75)
  - Panel divider between center and detail panel is approximately at 2/3 window width
  - Terminal toggle uses ctrl+grave (backtick)
  - Use reset_window_size() in setUp to ensure consistent starting state
---

## 2026-03-17 - US-GT-011
- Implemented test_detail_panel_modes.py — tests hybrid detail panel inline vs overlay modes
- Tests at 1500px (inline mode, above 1400px breakpoint) and 1100px (overlay mode, below breakpoint)
- Verifies Escape dismisses overlay, and window restores to default size
- Files changed: scripts/gui-tests/tests/test_detail_panel_modes.py
- **Learnings for future iterations:**
  - DETAIL_PANEL_BREAKPOINT is 1400px — use 1500px+ for inline mode testing, 1100px for overlay
  - Always re-read window bounds after resize_window() since coordinates change
  - Click commit again after resize to ensure detail panel shows at new size
---

## 2026-03-17 - US-GT-012
- Implemented test_commit_graph.py verifying SVG commit graph rendering
- Added `scroll()` helper to GUITest framework (Button4/Button5 X11 mouse wheel events)
- Test covers: graph overview with nodes/lanes/glow, commit row details (hash, message, author, date), branch/tag pill badges, scroll down/up virtualization check
- Files changed: scripts/gui-tests/tests/test_commit_graph.py (new), scripts/gui-tests/framework.py (added scroll method)
- **Learnings for future iterations:**
  - X11 mouse scroll uses Button4 (up) and Button5 (down) with press/release pairs
  - The scroll() helper is now available on GUITest base class for all tests
  - react-window virtualized lists need scroll testing to verify no gaps appear
---

## 2026-03-18 - US-GT-013
- Implemented test for commit graph interaction (click, context menu, keyboard navigation, hover)
- Files changed: scripts/gui-tests/tests/test_commit_interaction.py (new), scripts/gui-tests/framework.py (added hover method)
- **Learnings for future iterations:**
  - Added `hover(rel_x, rel_y)` method to GUITest base class in framework.py — moves mouse without clicking, useful for tooltip testing
  - The framework had no hover method before; now all future tests can use `self.hover(x, y)` for hover interactions
  - Context menu tests should click a commit first to establish focus, then right-click for context menu
  - Keyboard navigation tests need an initial click to give the graph focus before arrow keys work
---

## 2026-03-18 - US-GT-014
- Implemented test_detail_panel.py verifying commit detail panel content
- Tests: clicking commit opens detail panel with subject, SHA, author, date; Changed Files section with Lucide icons; SHA copy button feedback; close button dismisses panel
- Files changed: scripts/gui-tests/tests/test_detail_panel.py (new)
- **Learnings for future iterations:**
  - Detail panel opens on right side when clicking a commit row at ~y=130 (second row)
  - SHA copy button area is near x=85% of window width, y=160
  - Close button is at top-right corner of panel (w-20, 80)
  - The --filter flag matches both TestDetailPanel and TestDetailPanelModes — be aware of partial name matches
---

## 2026-03-18 - US-GT-015
- Implemented TestDiffViewer test class in scripts/gui-tests/tests/test_diff_viewer.py
- Test opens a test repo fixture, clicks a commit to open detail panel
- Clicks a file in Changed Files list to open diff view
- Asserts inline diff with green additions and red deletions
- Clicks Split toggle for side-by-side view, takes screenshot
- Uses ] key to navigate to next file diff
- Switches back to Inline mode
- Closes detail panel with Escape
- All 6 screenshots captured and saved as baselines
- Files changed: scripts/gui-tests/tests/test_diff_viewer.py (new), baselines/TestDiffViewer/ (6 screenshots)
- **Learnings for future iterations:**
  - Detail panel file list items are around y=45% of window height, x=78% for the right-side panel
  - Split/Inline toggle is near top of diff area at approximately y=32% of window height
  - ] key can be used for next-file navigation in diff view
  - Escape closes the detail panel/overlay
---

## 2026-03-18 - US-GT-016
- Created scripts/gui-tests/tests/test_staging.py with TestStaging class
- Test opens test repo (with staged/unstaged/untracked files from fixtures)
- Takes screenshots and asserts: staging area two-column layout, file count badges, Lucide icons
- Tests stage (+) button moves file from unstaged to staged column
- Tests unstage (-) button moves file back
- Tests Stage All and Unstage All bulk operations
- Files changed: scripts/gui-tests/tests/test_staging.py
- **Learnings for future iterations:**
  - The test repo fixture provides 1 staged file (staged_new.py), 1 unstaged modified (config.json), 1 untracked (notes.txt) — ideal for staging area tests
  - Staging area is in the lower portion of the center panel, coordinates approximate: y = h - 120 for file rows, y = h - 180 for section headers
  - Stage/unstage buttons appear on hover — use hover() before clicking
---

## 2026-03-18 - US-GT-017
- Implemented test for context menus on commits, branches, and staging area files
- Files changed: scripts/gui-tests/tests/test_context_menus.py (new)
- **Learnings for future iterations:**
  - Context menu tests follow same pattern: right-click target, assert_visual, Escape to close
  - Branch items in sidebar are at approximate x=100, y=155 (below section headers at ~125)
  - Staging area files are in the lower portion of center panel, unstaged at ~35% width, staged at ~65% width
  - Always test that dangerous/destructive menu items are styled in red
---

## 2026-03-18 - US-GT-018
- Implemented responsive layout test at 4 window sizes: 1280x800, 1024x768, 900x600, 800x500
- Files changed: scripts/gui-tests/tests/test_responsive.py (new)
- **Learnings for future iterations:**
  - Follow the same import boilerplate pattern as other tests (importlib.util for framework/fixtures)
  - Use resize_window() with wait(0.5) after each resize for responsive effects to settle
  - Press Ctrl+B twice to guarantee sidebar expanded state before assertions
  - reset_window_size() at end of test to clean up
---
