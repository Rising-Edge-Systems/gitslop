# Ralph Progress Log
Started: Mon Mar 16 11:36:31 PM EDT 2026
---

## Codebase Patterns
- Renderer components are in `src/renderer/src/components/`
- Use `lucide-react` for icons: `import { IconName } from 'lucide-react'` with `size` and `className` props
- Toolbar emoji icons use `<span className="toolbar-btn-icon">` wrapper pattern
- Typecheck: `npm run typecheck` (runs both node and web)
- PRD branch is `ralph/ui-overhaul`, created from `ralph/gitslop-v1`
- SidebarSection `icon` prop is `React.ReactNode` — pass icons as `icon={<IconName size={16} />}`
- Context menu icons: 14px, section header icons: 16px, inline small icons: 10-12px, status bar icons: 14px
- ContextMenuItem.icon is `React.ReactNode` — pass Lucide components like `<Pencil size={14} />`
- All emoji/unicode icons replaced with Lucide as of US-042c — zero emoji/unicode remaining in renderer
- `→` in description strings and `&#123;`/`&#125;` for JSX curly braces are NOT icons — leave them
- Design tokens in `src/renderer/src/styles/tokens.css` — use `--space-{xs,sm,md,lg,xl,2xl}`, `--font-{xs,sm,md,lg,xl}`, `--radius-{sm,md,lg}`, `--shadow-{sm,md,lg}`, `--transition-{fast,normal}` instead of hardcoded values
- tokens.css is imported BEFORE global.css in main.tsx — all color/spacing/typography vars live there
- CSS modules: `import styles from './Component.module.css'` then `className={styles.myClass}` — type declarations in `src/renderer/src/css-modules.d.ts`
- CSS module class names use camelCase (e.g., `styles.btnActive`) instead of kebab-case
- Shared dialog styles (`branch-dialog-*`) stay in global.css — don't move them into component modules
- `branch-dialog-btn-primary`, `branch-dialog-btn-secondary`, `branch-dialog-btn-danger` are shared across Toolbar, Sidebar, MergeDialog, RebaseDialog, SearchPalette
- `branch-ctx-menu-*` styles are shared across Sidebar, FileTree, ContextMenu — keep in global.css
- FileTree.tsx imports sidebarStyles for shared classes (placeholder, searchBox, searchInput, searchClear) — update FileTree when modifying sidebar styles

## 2026-03-16 - US-041
- Installed lucide-react (^0.577.0) as a dependency
- Replaced settings gear emoji (⚙) in Toolbar.tsx with Lucide `Settings` icon (size=18, className="lucide-icon")
- Files changed: package.json, package-lock.json, src/renderer/src/components/Toolbar.tsx
- **Learnings for future iterations:**
  - lucide-react icons accept `size` and `className` props directly
  - Toolbar buttons use `<span className="toolbar-btn-icon">` as a wrapper for icons
  - The SettingsPanel.tsx also has a gear emoji (line 36) in its tab config — will need updating in US-042c
  - FileTree.tsx has unicode gear symbols for yml/rs file types — these are file type indicators, not UI icons
---

## 2026-03-16 - US-042a
- Replaced all emoji/unicode icons in Toolbar.tsx with Lucide icons: ArrowDownToLine (pull), ArrowUpFromLine (push), RefreshCw (fetch), GitBranch (branch), GitMerge (merge), Archive (stash), Loader2 (spinner), AlertTriangle (warning), Check/XCircle (notifications)
- Replaced all emoji/unicode icons in TitleBar.tsx with Lucide icons: Sun/Moon (theme toggle), Minus (minimize), Square/Copy (maximize/restore), X (close), "GS" text wordmark (logo)
- Files changed: src/renderer/src/components/Toolbar.tsx, src/renderer/src/components/TitleBar.tsx
- **Learnings for future iterations:**
  - Toolbar icons use size={18}, TitleBar icons use size={14} for consistent sizing
  - Loader2 icon is better than RefreshCw for spinner animations (has CSS animation support)
  - The force push dialog warning icon was also an emoji (⚠️) — don't forget dialog content when replacing icons
  - TitleBar logo replaced with "GS" text wordmark rather than an icon
---

## 2026-03-16 - US-042b
- Replaced all emoji/unicode icons in Sidebar.tsx with Lucide icons: ChevronRight (collapse/expand chevrons), GitBranch (branches section/tab), Globe (remotes), Tag (tags), Archive (stashes), Package (submodules), FolderOpen (files tab), ArrowRightLeft (checkout), Pencil (rename), GitMerge (merge), RotateCcw (rebase), ArrowUpFromLine (push), Trash2 (delete), X (close buttons), AlertTriangle (errors), RefreshCw (fetch), Check (checkout tag, submodule status), Circle/CircleDot (submodule status), ArrowUp/ArrowDown (ahead/behind), Download/ExternalLink (submodule actions), CornerRightUp/CornerUpRight (stash apply/pop)
- Replaced all emoji/unicode icons in StatusBar.tsx with Lucide icons: GitBranch (branch), ArrowUp/ArrowDown (ahead/behind), ArrowDownToLine (incoming), Loader2 (spinners), RefreshCw (refresh), Bell (notifications), Check/XCircle/AlertTriangle/Info (notification types)
- Changed SidebarSectionProps.icon type from `string` to `React.ReactNode` to support JSX icon elements
- Files changed: src/renderer/src/components/Sidebar.tsx, src/renderer/src/components/StatusBar.tsx
- **Learnings for future iterations:**
  - SidebarSection `icon` prop is now `React.ReactNode`, pass icons as `icon={<IconName size={16} />}`
  - Context menu icons use 14px size, section icons 16px, inline small icons 10-12px
  - `stash@&#123;...&#125;` in Sidebar.tsx are JSX escapes for curly braces, not emoji — don't replace
  - `→` in title attributes is a text arrow for tooltips, not an icon — don't replace
  - Sidebar has many nested context menu components (BranchContextMenu, RemoteContextMenu, RemoteBranchContextMenu, TagsSection, StashesSection, SubmodulesSection) — each has its own icon replacements
---
## 2026-03-17 - US-042c
- Replaced ALL emoji/unicode icons in every remaining renderer component with Lucide icons
- Files changed: NotificationToast.tsx, StatusPanel.tsx, CommitGraph.tsx, WelcomeScreen.tsx, SearchPalette.tsx, SettingsPanel.tsx, BlameView.tsx, ConflictResolver.tsx, Terminal.tsx, RebaseDialog.tsx, MergeDialog.tsx, KeyboardShortcutsPanel.tsx, BottomPanel.tsx, CommitFilterBar.tsx, RepoView.tsx, ResetDialog.tsx, DiffViewer.tsx, CodeEditor.tsx, FileTree.tsx, CloneDialog.tsx, Sidebar.tsx (missed tag icon), ContextMenu.tsx (icon prop type)
- Changed ContextMenuItem.icon type from `string` to `React.ReactNode` to support JSX Lucide icons
- Changed StatusSection.stageActionIcon type from `string` to `React.ReactNode`
- Grep for emoji/unicode patterns returns zero matches in src/renderer/src/
- **Learnings for future iterations:**
  - `→` in description strings (like "Branch → upstream") are text, not icons — leave them as-is
  - `&#123;` and `&#125;` in Sidebar.tsx are JSX-safe curly braces for stash display, not icons
  - `\u2026` (ellipsis) in BlameView is text truncation, not an icon
  - FileTree getFileIcon returns ReactNode now, not string — downstream consumers already used it in JSX so no changes needed
  - Status indicators (M, S, U, !, D, A, R) in FileTree are letter abbreviations, not emoji — keep as strings
  - ContextMenuItem.icon is now React.ReactNode — pass Lucide components with size={14}
  - `−` (U+2212 minus sign) was used in button text labels — replace with <Minus> icon
---
## 2026-03-17 - US-043a
- Created `src/renderer/src/styles/tokens.css` with design tokens: spacing (--space-xs to --space-2xl), font sizes (--font-xs to --font-xl), border radius (--radius-sm/md/lg), shadows (--shadow-sm/md/lg), transitions (--transition-fast/normal)
- Moved all color variables from global.css :root and [data-theme="light"] into tokens.css
- Both dark and light theme values defined in tokens.css via [data-theme="light"] selector
- Light theme also gets adjusted shadow values (less opacity)
- tokens.css imported before global.css in main.tsx
- global.css now starts with a comment pointing to tokens.css, then the reset/global styles
- Files changed: src/renderer/src/styles/tokens.css (new), src/renderer/src/styles/global.css, src/renderer/src/main.tsx
- **Learnings for future iterations:**
  - tokens.css is imported BEFORE global.css in main.tsx — order matters for cascading
  - Design tokens available: --space-{xs,sm,md,lg,xl,2xl}, --font-{xs,sm,md,lg,xl}, --radius-{sm,md,lg}, --shadow-{sm,md,lg}, --transition-{fast,normal}
  - When creating CSS modules, use design tokens instead of hardcoded px values
  - global.css no longer has :root or [data-theme] blocks — all in tokens.css
---
## 2026-03-17 - US-043b
- Extracted Toolbar styles from global.css into Toolbar.module.css (toolbar, buttons, progress, notifications, force-push warning, pull strategy, stash checkbox)
- Extracted TitleBar styles from global.css into TitleBar.module.css (titlebar, brand, repo display, window controls)
- Extracted StatusBar styles from global.css into StatusBar.module.css (status bar, branch info, sync, operations, notifications, history dropdown)
- Created css-modules.d.ts type declaration for TypeScript CSS module support
- Updated all three components to use scoped `styles.className` pattern
- Added missing `branch-dialog-btn-primary`, `branch-dialog-btn-secondary`, `branch-dialog-btn-danger` styles to global.css (shared across multiple components)
- Fixed broken `toolbar-spinner-rotate` keyframe reference in status bar — defined proper `spinnerRotate` keyframe in module
- Files changed: Toolbar.tsx, Toolbar.module.css (new), TitleBar.tsx, TitleBar.module.css (new), StatusBar.tsx, StatusBar.module.css (new), css-modules.d.ts (new), global.css
- **Learnings for future iterations:**
  - CSS modules need a type declaration file (`*.module.css` → `declare module`) for TypeScript
  - Shared dialog classes (branch-dialog-*) must stay in global.css, not in component modules
  - `branch-dialog-btn-primary/secondary/danger` were previously only defined in the toolbar section — they needed to be added back near the other branch-dialog styles
  - The original `toolbar-spinner-rotate` keyframe was never defined in global.css (broken reference) — fixed by defining `spinnerRotate` in StatusBar.module.css
  - When converting to CSS modules, keep global class names for shared dialogs but use module classes for component-specific styles
  - `@keyframes slideUp` is shared by multiple components — keep in global.css
---
## 2026-03-17 - US-043c
- Created `Sidebar.module.css` with all sidebar-related styles extracted from global.css
- Converted all sidebar-specific classNames in Sidebar.tsx to use `styles.xxx` scoped pattern
- Updated FileTree.tsx to import `sidebarStyles` from Sidebar.module.css for shared classes (placeholder, searchBox, searchInput, searchClear)
- Removed all sidebar-*, stash-diff-*, sidebar-submodule-* styles from global.css
- Kept branch-ctx-menu-* and branch-dialog-* styles in global.css (shared across Sidebar, FileTree, ContextMenu, MergeDialog, etc.)
- Replaced hardcoded pixel values with design tokens (--space-*, --font-*, --radius-*, --transition-*)
- Files changed: Sidebar.module.css (new), Sidebar.tsx, FileTree.tsx, global.css
- **Learnings for future iterations:**
  - FileTree.tsx uses some sidebar classes (placeholder, search-box/input/clear) — when extracting sidebar styles, also update FileTree
  - Cross-component imports of CSS modules are fine: `import sidebarStyles from './Sidebar.module.css'` works for sharing scoped styles
  - branch-ctx-menu-* and branch-dialog-* are shared across 5+ components — keep in global.css until a shared module is created
  - The `.sidebar-section-title` responsive rule in the @media query also needed removal from global.css
  - Stash items use a `selected` class conditionally — handle with separate camelCase class (stashItemSelected)
---
