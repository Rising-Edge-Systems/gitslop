# Ralph Progress Log
Started: Thu Apr 10 2026
---

## Codebase Patterns
- CommitGraph.tsx: loadCommits() fetches git log with { all: true }, no maxCount limit
- Skip-if-unchanged: lines 1047-1053 compare first+last hash and length — misses ref changes after push
- onRepoChanged: file watcher fires when working tree files change — does NOT fire after push/fetch/pull (only remote refs change)
- Toolbar.tsx: push/fetch/pull operations show notifications on success but don't trigger graph refresh
- git-service.ts log(): accepts maxCount option — already supported, just not used
- Lane assignment: laneAssignment.ts assignLanes() assigns lanes per branch divergence — no lane collapsing for inactive branches
- CANVAS_THRESHOLD: 50000 commits triggers canvas renderer instead of SVG
- Settings: useSettings() hook, settings persisted to localStorage
- StatusBar: listens to onRepoChanged for ahead/behind updates
- Sidebar: listens to onRepoChanged for branch list updates
- Typecheck: npm run typecheck
- Build: npx electron-vite build && npx electron --no-sandbox .
---
