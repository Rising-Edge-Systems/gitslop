# Ralph Progress Log
Started: Mon Mar 16 01:03:27 PM EDT 2026
---

## Codebase Patterns
- Use `electron-vite` v4 with `vite` v6 (v5+ has peer dep conflicts with electron-vite v5)
- Project uses separate tsconfigs: `tsconfig.node.json` (main+preload), `tsconfig.web.json` (renderer)
- Run `npm run typecheck` to check both node and web TypeScript
- electron-vite v4 doesn't export `is` from `electron-vite/node`; use `!app.isPackaged` instead for dev detection
- Build output goes to `out/` directory (electron-vite default)
- NVM must be sourced before using node: `export NVM_DIR="$HOME/.nvm" && [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"`
- Custom titlebar with frameless window; IPC channels prefixed with `window:`, `dialog:`, `git:`, `repos:`
- Global `Window` interface with `electronAPI` is declared in `src/renderer/src/hooks/useLayoutState.ts` (single declaration)
- electron-store used in main process for persisting recent repos (schema typed with `Store<StoreSchema>`)
- `RecentRepo` type exported from `useLayoutState.ts` for renderer use
- CSS uses custom properties (CSS variables) defined in `:root` in `src/renderer/src/styles/global.css`
- React components go in `src/renderer/src/components/`
- Custom hooks go in `src/renderer/src/hooks/`
- `react-resizable-panels` v4.7+ uses `Group`, `Panel`, `Separator` (not `PanelGroup`/`PanelResizeHandle`)
- `react-resizable-panels` v4 `onResize` callback receives `PanelSize` object with `.asPercentage` and `.inPixels`
- Layout state persisted via `localStorage` with key `gitslop-layout-state`
- GitService singleton in `src/main/git-service.ts` — all git operations go through it
- Git IPC handlers registered via `registerGitIpcHandlers()` in `src/main/git-ipc.ts`
- All git IPC results use `{ success: boolean, data?, error?, code? }` pattern (GitServiceResult)
- vitest is configured; run `npm test` — no config file needed for Node.js tests
- Test files go in `__tests__/` subdirectories next to source files
- `execFile` callback stdout/stderr can be `string | Buffer` — always use `?.toString() ?? ''`
- `react-window` v2 uses `List` (not `FixedSizeList`); API: `rowComponent` + `rowProps`, not children render
- `react-window` v2 `rowComponent` receives `{ ariaAttributes, index, style } & RowProps`
- `useListCallbackRef` hook for imperative List API in react-window v2
- `Sidebar` component accepts `currentRepo` prop; each sidebar section loads its own data when repo is set
- Git branch operations (checkout, create, delete, rename) available via `window.electronAPI.git.checkout/createBranch/deleteBranch/renameBranch`
- File watcher: `watcher:start`/`watcher:stop` IPC in main, `repo:changed` event to renderer, `onRepoChanged` callback in preload
- New IPC APIs: add to preload, then update Window.electronAPI type in `useLayoutState.ts`
---

## 2026-03-16 13:10 - US-001
- Implemented: Full Electron + React + TypeScript scaffold with electron-vite
- Files created:
  - `package.json` - Project config with dev/build/typecheck scripts
  - `electron-vite.config.ts` - Build configuration for main, preload, renderer
  - `tsconfig.json`, `tsconfig.node.json`, `tsconfig.web.json` - TypeScript configs (strict mode)
  - `src/main/index.ts` - Main process: frameless window, IPC for window controls
  - `src/preload/index.ts` - Preload: contextBridge with typed electronAPI
  - `src/renderer/index.html` - Entry HTML with CSP
  - `src/renderer/src/main.tsx` - React entry point
  - `src/renderer/src/App.tsx` - Root component with TitleBar
  - `src/renderer/src/components/TitleBar.tsx` - Custom titlebar with min/max/close
  - `src/renderer/src/styles/global.css` - Dark theme with CSS custom properties
- **Learnings for future iterations:**
  - electron-vite v4 is required for vite v6 compatibility (v5 needs vite 7+/8+ which has conflicts)
  - `is.dev` from electron-vite/node doesn't exist in v4; use `!app.isPackaged` instead
  - Must source NVM in every bash command since shell state doesn't persist
  - electron-vite outputs to `out/` by default (main, preload, renderer subdirs)
  - Renderer root is at `src/renderer/` and its build output needs `../../out/renderer` path
---

## 2026-03-16 13:25 - US-002
- Implemented: Application layout with sidebar, toolbar, main content, and bottom panel
- Files changed/created:
  - `src/renderer/src/App.tsx` - Updated to use AppLayout
  - `src/renderer/src/components/AppLayout.tsx` - Main layout with react-resizable-panels (Group/Panel/Separator)
  - `src/renderer/src/components/Toolbar.tsx` - Top toolbar with Pull, Push, Fetch, Branch, Merge, Stash, Settings buttons
  - `src/renderer/src/components/Sidebar.tsx` - Left sidebar with collapsible Branches, Remotes, Tags, Stashes sections
  - `src/renderer/src/components/BottomPanel.tsx` - Toggleable bottom panel (terminal placeholder)
  - `src/renderer/src/components/MainContent.tsx` - Center content area (welcome placeholder)
  - `src/renderer/src/hooks/useLayoutState.ts` - Layout state management with localStorage persistence
  - `src/renderer/src/styles/global.css` - All new layout CSS styles
  - `package.json` - Added react-resizable-panels, electron-store dependencies
- **Learnings for future iterations:**
  - react-resizable-panels v4.7 uses `Group`/`Separator` not `PanelGroup`/`PanelResizeHandle`
  - `onResize` in v4 receives `PanelSize` object (not bare number), use `.asPercentage`
  - No `order` prop on Panels in v4; ordering is by DOM position
  - Layout persistence uses localStorage (simpler than electron-store for renderer-side state)
  - Ctrl+B toggles sidebar, Ctrl+` toggles bottom panel
---

## 2026-03-16 13:40 - US-003
- Implemented: Welcome screen with Open/Clone/Init buttons, recent repos list with persistence
- Files changed/created:
  - `src/main/index.ts` - Added IPC handlers for dialog:openDirectory, git:isRepo, git:init, repos:getRecent/addRecent/removeRecent
  - `src/preload/index.ts` - Added dialog, git, repos APIs to electronAPI bridge
  - `src/renderer/src/components/WelcomeScreen.tsx` - New welcome screen component with action buttons and recent repos
  - `src/renderer/src/components/MainContent.tsx` - Now accepts currentRepo/onRepoOpen props, shows WelcomeScreen when no repo open
  - `src/renderer/src/components/AppLayout.tsx` - Passes currentRepo/onRepoOpen to MainContent
  - `src/renderer/src/App.tsx` - Manages currentRepo state, passes to AppLayout
  - `src/renderer/src/hooks/useLayoutState.ts` - Now contains global Window/electronAPI type declaration (moved from TitleBar)
  - `src/renderer/src/components/TitleBar.tsx` - Removed duplicate Window declaration
  - `src/renderer/src/styles/global.css` - Added welcome screen styles (hero, actions, recent repos, error banner)
- **Learnings for future iterations:**
  - Global `Window` electronAPI declaration lives in `useLayoutState.ts` — add new IPC APIs there
  - electron-store is used in main process only; renderer uses IPC to access stored data
  - Recent repos stored as array of {path, name, lastOpened} in electron-store
  - `execFile` from child_process is used for git commands (promisified)
  - Clone dialog is a placeholder — will be implemented in US-006
---

## 2026-03-16 13:55 - US-004
- Implemented: Git CLI wrapper service with command queue, cancellation, structured output, error classification, and version detection
- Files created:
  - `src/main/git-service.ts` - GitService class with queue, AbortController, structured parsers for log/branches/remotes/tags/stashes/status/diff
  - `src/main/git-ipc.ts` - IPC handlers bridging GitService to renderer (registerGitIpcHandlers)
  - `src/main/__tests__/git-service.test.ts` - 27 tests covering version detection, repo ops, log, branches, remotes, tags, stashes, status, diff, error handling, cancellation, queue
- Files modified:
  - `src/main/index.ts` - Replaced inline git handlers with git-ipc module registration, added version check on startup
  - `src/preload/index.ts` - Added GitServiceResult type and all new git IPC methods
  - `src/renderer/src/hooks/useLayoutState.ts` - Updated Window.electronAPI type with all new git methods
  - `package.json` - Added vitest, test script
- **Learnings for future iterations:**
  - vitest works out of the box with no config needed for Node.js tests
  - execFile callback gives `string | Buffer` for stdout/stderr — always call `.toString()` with null safety
  - ExecFileException's `code` field is `string | number | null | undefined`, not just `string | undefined`
  - `registerGitIpcHandlers()` removes old handlers before re-registering to avoid conflicts with US-003 handlers
  - GitService uses a per-repo command queue (Map<repoPath, QueueItem[]>) to prevent concurrent git operations
  - All IPC git results use `{ success, data?, error?, code? }` pattern (GitServiceResult)
  - `git status --porcelain=v2 --branch -u` gives machine-parseable status with branch info
  - `git log --format` with custom separators (<<<SEP>>>) gives structured commit data
---

## 2026-03-16 14:02 - US-005
- Implemented: Open & Init repositories with proper repo view, titlebar repo path display, and fixed "Init from Error" flow
- Files changed/created:
  - `src/renderer/src/components/RepoView.tsx` - New repo view component showing branch, status summary, branch list
  - `src/renderer/src/components/TitleBar.tsx` - Added repoPath prop, displays repo name and path in titlebar
  - `src/renderer/src/components/MainContent.tsx` - Added onCloseRepo prop, uses RepoView instead of placeholder
  - `src/renderer/src/components/AppLayout.tsx` - Passes onCloseRepo through to MainContent
  - `src/renderer/src/components/WelcomeScreen.tsx` - Fixed "Init from Error" to use stored errorPath instead of re-opening picker
  - `src/renderer/src/App.tsx` - Added handleCloseRepo, passes repoPath to TitleBar
  - `src/renderer/src/styles/global.css` - Added titlebar repo display styles, full RepoView styles, spin animation
- **Learnings for future iterations:**
  - TitleBar accepts optional `repoPath` prop to show current repo in titlebar
  - RepoView component loads status and branches via git IPC on mount
  - MainContent now requires `onCloseRepo` prop for returning to welcome screen
  - CSS `:has()` selector used for conditional centering (welcome screen vs repo view)
  - WelcomeScreen stores `errorPath` separately from error message to enable "Init Here" on the same path
---

## 2026-03-16 14:06 - US-006
- Implemented: Clone Repository Dialog with progress reporting, cancellation, and error handling
- Files created:
  - `src/renderer/src/components/CloneDialog.tsx` - Modal dialog with URL, destination, name fields; progress bar; cancel support
- Files modified:
  - `src/main/git-service.ts` - Added `clone()` method using `spawn` for real-time progress, `CloneProgress` type, `parseCloneProgress()`
  - `src/main/git-ipc.ts` - Added `git:clone` IPC handler with progress events via `BrowserWindow.webContents.send`
  - `src/preload/index.ts` - Added `clone()` and `onCloneProgress()` to electronAPI
  - `src/renderer/src/hooks/useLayoutState.ts` - Updated Window type with clone/onCloneProgress methods
  - `src/renderer/src/components/WelcomeScreen.tsx` - Replaced placeholder with CloneDialog integration
  - `src/renderer/src/styles/global.css` - Added full clone dialog styles (overlay, form, progress bar, buttons)
- **Learnings for future iterations:**
  - Use `spawn` (not `execFile`) for commands needing real-time progress output
  - Clone progress sent via IPC events (`git:clone-progress`) from main to renderer using `BrowserWindow.webContents.send`
  - Git clone progress comes on stderr, not stdout; parse with regex for phase/percent/count
  - Clone dialog is rendered as a modal overlay with fixed positioning and z-index: 1000
  - `operationIdRef` tracks the active clone operation for progress matching and cancellation
---

## 2026-03-16 14:13 - US-007
- Implemented: Visual commit graph with Canvas rendering, virtualized scrolling, branch labels, and auto-refresh
- Files created:
  - `src/renderer/src/components/CommitGraph.tsx` - Full commit graph component with:
    - Graph layout algorithm computing lane positions for branches/merges
    - Canvas-based line rendering (bezier curves for merges, straight lines for linear history)
    - react-window v2 `List` for virtualized scrolling (handles 100k+ commits)
    - Colored branch lanes (10-color Catppuccin palette)
    - Branch/tag/remote/HEAD ref badges on commits
    - Merge commit visual distinction (hollow circles)
    - HEAD commits highlighted with white ring
    - Auto-refresh every 5 seconds
    - ResizeObserver for dynamic container height
- Files modified:
  - `src/renderer/src/components/RepoView.tsx` - Integrated CommitGraph below status summary
  - `src/renderer/src/styles/global.css` - Added comprehensive commit graph styles (rows, refs, canvas, viewport)
  - `package.json` - Added react-window dependency
- **Learnings for future iterations:**
  - react-window v2 (latest) uses `List` not `FixedSizeList`; API is `rowComponent` + `rowProps` (not children render function)
  - react-window v2 `rowComponent` receives `{ ariaAttributes, index, style } & RowProps`
  - `useListCallbackRef` hook replaces `useRef<List>()` for imperative List API access
  - `onRowsRendered` gives `(visibleRows, allRows)` where each has `{ startIndex, stopIndex }`
  - Canvas graph is rendered as absolute overlay, list rows have transparent graph lane space
  - Graph layout: activeLanes array tracks which hashes are "running" in each column
  - repo-view-content needs `flex: 1; min-height: 0; overflow: hidden` for graph to fill available space
---

## 2026-03-16 14:18 - US-008
- Implemented: Commit graph interaction — click-to-select, detail panel, context menu, keyboard navigation
- Files changed:
  - `src/renderer/src/components/CommitGraph.tsx` - Added selection state, CommitDetailPanel, CommitContextMenu, keyboard nav (arrow keys), exported CommitDetail type
  - `src/renderer/src/styles/global.css` - Added styles for: selected row, context menu, commit detail panel with metadata/files, wrapper layout
- **Learnings for future iterations:**
  - react-window v2 `scrollToRow` takes an object `{ index, align, behavior }`, not positional args
  - CommitGraph now exports `CommitDetail` type for use by parent components
  - CommitGraph uses a wrapper div (`.commit-graph-wrapper`) with flex layout to show detail panel alongside
  - Context menu actions (cherry-pick, revert, reset, create-branch, create-tag) are placeholders — only copy-sha is functional
  - `onCommitSelect` callback prop allows parent to react to selection changes
  - Detail panel loads via `git:showCommit` IPC which returns commit metadata + changed files list
  - `useCallback` on row click/context handlers is important — they're passed as rowProps to react-window
---

## 2026-03-16 14:23 - US-009
- Implemented: Branch sidebar with full list & management — search/filter, double-click checkout, right-click context menu, new branch dialog, rename dialog, ahead/behind tracking
- Files changed:
  - `src/main/git-service.ts` - Added `checkout()`, `createBranch()`, `deleteBranch()`, `renameBranch()` methods
  - `src/main/git-ipc.ts` - Added IPC handlers for `git:checkout`, `git:createBranch`, `git:deleteBranch`, `git:renameBranch`
  - `src/preload/index.ts` - Added checkout, createBranch, deleteBranch, renameBranch to electronAPI.git
  - `src/renderer/src/hooks/useLayoutState.ts` - Updated Window.electronAPI type with new git branch methods
  - `src/renderer/src/components/Sidebar.tsx` - Full rewrite: branches from git, search filter, context menu, new branch dialog, rename dialog
  - `src/renderer/src/components/AppLayout.tsx` - Pass currentRepo to Sidebar
  - `src/renderer/src/styles/global.css` - Added branch sidebar styles (search, items, context menu, dialogs)
- **Learnings for future iterations:**
  - Sidebar now accepts `currentRepo` prop — all sidebar sections need it to load data
  - SidebarSection component supports `count` and `headerAction` props for enhanced headers
  - Branch context menu actions for merge/rebase are placeholders using `git:exec` — will be proper in US-019/US-020
  - Sidebar auto-refreshes branches every 5 seconds (same pattern as CommitGraph)
  - Search filter only appears when there are >5 branches to avoid clutter
  - `confirm()` is used for delete confirmations — simple but works in Electron
---

## 2026-03-16 14:27 - US-010
- Implemented: Remote branches sidebar with expandable remotes, remote branch listing, context menus, Add/Edit/Remove remote dialogs, fetch, checkout remote branch, delete remote branch
- Files changed:
  - `src/main/git-service.ts` - Added `getRemoteBranches()`, `addRemote()`, `editRemoteUrl()`, `removeRemote()`, `fetch()`, `deleteRemoteBranch()`, `checkoutRemoteBranch()` methods
  - `src/main/git-ipc.ts` - Added IPC handlers for all new remote operations
  - `src/preload/index.ts` - Added all new remote IPC methods to electronAPI
  - `src/renderer/src/hooks/useLayoutState.ts` - Updated Window.electronAPI type with new remote methods
  - `src/renderer/src/components/Sidebar.tsx` - Added full RemotesSection component with expandable remotes, remote branch listing, context menus (fetch/edit/remove for remotes, checkout/fetch/delete for branches), Add/Edit remote dialogs
  - `src/renderer/src/styles/global.css` - Added sidebar remote list, remote group, remote item, remote branch item styles
- **Learnings for future iterations:**
  - Remote branches use `git branch -r --format` to get structured data, filtering out HEAD entries
  - `checkoutRemoteBranch` uses `git checkout -b <branch> <remote>/<branch>` to create a local tracking branch
  - RemotesSection is a separate component from the main Sidebar, receives `currentRepo` and `onBranchesChanged` props
  - Remote context menus reuse the same `branch-ctx-menu` CSS classes from the branch context menu
  - Dialog components reuse the `branch-dialog-*` CSS classes for consistency
---
## 2026-03-16 14:30 - US-011
- Implemented: Tags sidebar with full list, search/filter, right-click context menu (delete/push), new tag dialog (lightweight & annotated), relative date display
- Files changed:
  - `src/main/git-service.ts` - Added `createTag()`, `deleteTag()`, `pushTag()` methods
  - `src/main/git-ipc.ts` - Added IPC handlers for `git:createTag`, `git:deleteTag`, `git:pushTag`
  - `src/preload/index.ts` - Added createTag, deleteTag, pushTag to electronAPI.git
  - `src/renderer/src/hooks/useLayoutState.ts` - Updated Window.electronAPI type with new tag methods
  - `src/renderer/src/components/Sidebar.tsx` - Added TagsSection component replacing placeholder (search filter, context menu, new tag dialog with message textarea for annotated tags)
  - `src/renderer/src/styles/global.css` - Added tag sidebar item styles (sidebar-tag-item, sidebar-tag-name, sidebar-tag-date, sidebar-tag-icon)
- **Learnings for future iterations:**
  - TagsSection follows same pattern as RemotesSection — separate component with its own state, receives `currentRepo` prop
  - Tags reuse `branch-ctx-menu` and `branch-dialog-*` CSS classes for context menus and dialogs
  - `git tag -a <name> -m <message>` creates annotated tags; without `-a`/`-m` creates lightweight
  - Tags auto-refresh every 5 seconds (same pattern as branches and remotes)
  - Search filter only appears when there are >5 tags to avoid clutter
---
## 2026-03-16 14:33 - US-012
- Implemented: Stash sidebar with full list, click-to-view-diff, right-click context menu (apply/pop/drop), stash save dialog (with message and include-untracked options), toolbar stash button
- Files changed:
  - `src/main/git-service.ts` - Added `stashSave()`, `stashApply()`, `stashPop()`, `stashDrop()`, `stashShow()` methods
  - `src/main/git-ipc.ts` - Added IPC handlers for `git:stashSave`, `git:stashApply`, `git:stashPop`, `git:stashDrop`, `git:stashShow`
  - `src/preload/index.ts` - Added stashSave, stashApply, stashPop, stashDrop, stashShow to electronAPI.git
  - `src/renderer/src/hooks/useLayoutState.ts` - Updated Window.electronAPI type with new stash methods
  - `src/renderer/src/components/Sidebar.tsx` - Added StashesSection component replacing placeholder (stash list, context menu, diff viewer, save dialog)
  - `src/renderer/src/components/Toolbar.tsx` - Added currentRepo prop, stash button opens save dialog
  - `src/renderer/src/components/AppLayout.tsx` - Pass currentRepo to Toolbar
  - `src/renderer/src/styles/global.css` - Added stash sidebar, stash diff viewer, stash dialog checkbox styles
- **Learnings for future iterations:**
  - StashesSection follows same pattern as TagsSection/RemotesSection — separate component with its own state, receives `currentRepo` prop
  - Stash operations use `git stash push` (not `git stash save` which is deprecated)
  - Stash show uses `git stash show -p stash@{N}` for full diff output
  - Toolbar now accepts `currentRepo` prop — needed for any toolbar action that invokes git commands
  - Stash diff viewer is shown as a modal overlay (same pattern as clone dialog)
---
## 2026-03-16 14:37 - US-013
- Implemented: Working Directory Status Panel with three collapsible sections (Staged/Unstaged/Untracked), file diff viewer, file watcher for auto-refresh, and clean working directory empty state
- Files created:
  - `src/renderer/src/components/StatusPanel.tsx` - StatusPanel component with collapsible sections, file list with status icons, inline diff viewer, auto-refresh via polling + file watcher
- Files modified:
  - `src/main/index.ts` - Added file watcher IPC handlers (watcher:start/stop) using Node.js `fs.watch` with recursive flag, debounced repo:changed events
  - `src/preload/index.ts` - Added watcher.start/stop and onRepoChanged IPC methods
  - `src/renderer/src/hooks/useLayoutState.ts` - Updated Window.electronAPI type with watcher and onRepoChanged
  - `src/renderer/src/components/RepoView.tsx` - Integrated StatusPanel, starts/stops file watcher on repo mount/unmount
  - `src/renderer/src/styles/global.css` - Added comprehensive StatusPanel styles (sections, file items, diff viewer, status icons)
- **Learnings for future iterations:**
  - File watcher uses Node.js native `fs.watch` with `{ recursive: true }` — simpler than chokidar, no extra dependency
  - Watcher IPC channels: `watcher:start`/`watcher:stop` in main, `repo:changed` event sent to renderer
  - StatusPanel polls every 3 seconds AND listens for file watcher events (belt and suspenders approach)
  - Status icons use color-coded CSS classes: `.status-icon-added` (green), `.status-icon-modified` (orange), `.status-icon-deleted` (red)
  - StatusPanel is embedded in RepoView between the summary cards and the CommitGraph
  - `onRepoChanged` IPC is added to the preload alongside `watcher` methods
---
## 2026-03-16 14:42 - US-014
- Implemented: Staging & Unstaging files with per-file buttons, Stage All/Unstage All, multi-select, drag-and-drop, keyboard shortcuts
- Files changed:
  - `src/main/git-service.ts` - Added `stageFiles()`, `unstageFiles()`, `stageAll()`, `unstageAll()` methods
  - `src/main/git-ipc.ts` - Added IPC handlers for `git:stageFiles`, `git:unstageFiles`, `git:stageAll`, `git:unstageAll`
  - `src/preload/index.ts` - Added stageFiles, unstageFiles, stageAll, unstageAll to electronAPI.git
  - `src/renderer/src/hooks/useLayoutState.ts` - Updated Window.electronAPI type with new staging methods
  - `src/renderer/src/components/StatusPanel.tsx` - Major rewrite: added stage/unstage buttons on each file (+/− icons), Stage All/Unstage All buttons in header, multi-select (Ctrl/Shift+click), drag-and-drop between sections, keyboard shortcuts (S to stage, U to unstage)
  - `src/renderer/src/styles/global.css` - Added styles for stage/unstage buttons, drop targets, action buttons, file info layout
- **Learnings for future iterations:**
  - `git restore --staged` is the modern way to unstage (replaces `git reset HEAD <file>`)
  - `git reset HEAD` (no file args) unstages everything
  - StatusPanel now uses `status-file-info` inner button + `status-stage-btn` for per-file actions
  - Drag-and-drop between staged/unstaged/untracked sections uses standard HTML5 drag API
  - Multi-select uses Set<string> with keys like "staged:path" or "unstaged:path" to distinguish sections
  - Keyboard shortcuts S (stage) and U (unstage) work when the status panel has focus
---
## 2026-03-16 14:47 - US-015
- Implemented: Hunk & Line Staging — per-hunk stage/unstage buttons in diff viewer, individual line selection with checkboxes, line-level staging/unstaging via git apply --cached
- Files changed:
  - `src/main/git-service.ts` - Added `stageHunk()`, `unstageHunk()`, and private `applyPatch()` methods using `spawn` to pipe patch data to `git apply --cached`
  - `src/main/git-ipc.ts` - Added IPC handlers for `git:stageHunk`, `git:unstageHunk`
  - `src/preload/index.ts` - Added stageHunk, unstageHunk to electronAPI.git
  - `src/renderer/src/hooks/useLayoutState.ts` - Updated Window.electronAPI type with stageHunk/unstageHunk
  - `src/renderer/src/components/StatusPanel.tsx` - Added HunkDiffViewer component with diff parser, hunk-level and line-level staging UI (parseDiff, buildHunkPatch, buildLinesPatch helpers)
  - `src/renderer/src/styles/global.css` - Added comprehensive hunk diff viewer styles (hunk blocks, line selection, checkboxes, action buttons)
- **Learnings for future iterations:**
  - `git apply --cached --unidiff-zero --whitespace=nowarn -` reads patch from stdin; use `spawn` and pipe patch to stdin
  - For unstaging hunks, use `git apply --cached --reverse` with the same patch
  - Diff parsing: file header lines (diff --git, index, ---, +++) must be preserved in patches
  - Line-level staging requires rebuilding the hunk: unselected added lines are dropped, unselected removed lines become context
  - `--unidiff-zero` allows applying patches with zero context lines (important for line-level staging)
  - HunkDiffViewer is a child of StatusPanel, receives operationInProgress state from parent
  - Line selection supports Shift+click for range selection within a hunk
---
## 2026-03-16 14:54 - US-016
- Implemented: Diff Viewer with side-by-side and inline (unified) modes, syntax highlighting, word-level diff, binary file handling, large file truncation, scroll sync
- Files created:
  - `src/renderer/src/components/DiffViewer.tsx` - Full diff viewer component with:
    - Toggle between inline and side-by-side views
    - Lightweight syntax highlighting for 20+ languages (keywords, strings, comments, numbers, functions, types)
    - Word-level diff highlighting (LCS-based token diff) within changed lines
    - Line numbers for both old and new versions
    - Color-coded diffs (green added, red removed, word-level highlights)
    - Scroll sync between left/right panes in side-by-side mode
    - Binary file detection with "Binary file changed" message
    - Large file truncation with "Show More" / "Show All" buttons (5000 line limit)
    - Side-by-side pairing algorithm that aligns removed/added blocks
- Files modified:
  - `src/main/git-service.ts` - Added `showCommitFileDiff()` method for getting diff of a specific file in a commit
  - `src/main/git-ipc.ts` - Added `git:showCommitFileDiff` IPC handler
  - `src/preload/index.ts` - Added `showCommitFileDiff` to electronAPI
  - `src/renderer/src/hooks/useLayoutState.ts` - Updated Window.electronAPI type with showCommitFileDiff
  - `src/renderer/src/components/StatusPanel.tsx` - Integrated DiffViewer via DiffViewerWithStaging wrapper (Staging/Enhanced View toggle tabs)
  - `src/renderer/src/components/CommitGraph.tsx` - Added DiffViewer integration in CommitDetailPanel (double-click file to view diff)
  - `src/renderer/src/styles/global.css` - Added comprehensive DiffViewer styles (inline, side-by-side, syntax tokens, word diff, binary, truncation, tabs)
- **Learnings for future iterations:**
  - DiffViewer is a standalone component that can be used anywhere diffs need to be displayed
  - Syntax highlighting is built-in (no external dependency) — tokenizer handles keywords, strings, comments, numbers for 20+ languages
  - Word-level diff uses LCS algorithm on tokenized words — skips if token count product > 50000 to avoid perf issues
  - Side-by-side view pairs removed/added lines by collecting consecutive blocks and aligning them
  - Scroll sync uses requestAnimationFrame with a syncing guard to prevent infinite scroll loops
  - `git show --format= --patch <hash> -- <file>` gets the diff of a specific file in a commit
  - DiffViewerWithStaging wraps HunkDiffViewer (staging controls) and DiffViewer (enhanced view) with a tab bar toggle
---
## 2026-03-16 14:57 - US-017
- Implemented: Commit Dialog with subject line + expandable body, char count indicator, Commit & Commit & Push buttons, amend checkbox, sign-off checkbox, Ctrl+Enter shortcut
- Files created:
  - `src/renderer/src/components/CommitDialog.tsx` - Full commit dialog component with subject/body inputs, amend pre-fill, sign-off, Commit & Push, keyboard shortcut
- Files modified:
  - `src/main/git-service.ts` - Added `commit()`, `getLastCommitMessage()`, `push()` methods
  - `src/main/git-ipc.ts` - Added IPC handlers for `git:commit`, `git:getLastCommitMessage`, `git:push`
  - `src/preload/index.ts` - Added commit, getLastCommitMessage, push to electronAPI.git
  - `src/renderer/src/hooks/useLayoutState.ts` - Updated Window.electronAPI type with new commit/push methods
  - `src/renderer/src/components/RepoView.tsx` - Integrated CommitDialog between StatusPanel and CommitGraph
  - `src/renderer/src/styles/global.css` - Added comprehensive CommitDialog styles
- **Learnings for future iterations:**
  - `git commit -m` for basic commit, `--amend` for amend, `--signoff` for sign-off
  - `git log -1 --format=%B` gets the full last commit message for amend pre-fill
  - CommitDialog is a standalone component that receives `stagedCount` from parent to control button state
  - Commit output format is `[branch hash] message` — parse with regex to get new commit hash
  - `push()` method added to GitService for Commit & Push convenience button
---
## 2026-03-16 15:01 - US-018
- Implemented: Push, Pull, Fetch operations with progress indicators, dialogs, keyboard shortcuts, and full error handling
- Files changed:
  - `src/main/git-service.ts` - Added `pull()`, `fetchWithProgress()`, `hasUpstream()`, `getCurrentBranch()` methods; enhanced `push()` with force/setUpstream options; added `execWithProgress()` and `parseGitProgress()` private helpers; added `GitOperationProgress` type
  - `src/main/git-ipc.ts` - Added IPC handlers for `git:pull`, `git:fetchWithProgress`, `git:hasUpstream`, `git:getCurrentBranch`; enhanced `git:push` handler with options and progress events
  - `src/preload/index.ts` - Added pull, fetchWithProgress, hasUpstream, getCurrentBranch, onOperationProgress to electronAPI.git
  - `src/renderer/src/hooks/useLayoutState.ts` - Updated Window.electronAPI type with new push/pull/fetch methods and onOperationProgress
  - `src/renderer/src/components/Toolbar.tsx` - Full rewrite: wired up Push/Pull/Fetch buttons with proper functionality, added force push confirmation dialog, set upstream dialog, pull strategy dialog (merge vs rebase), progress indicators with spinner, toast notifications, keyboard shortcuts (Ctrl+Shift+P/L/F)
  - `src/renderer/src/styles/global.css` - Added toolbar progress bar, spinner animation, notification toasts, force push warning, pull strategy radio options, upstream dialog styles
- **Learnings for future iterations:**
  - `execWithProgress()` is a reusable helper for any git command that needs real-time progress via `spawn`
  - Git progress comes on stderr; parse with regex for "Phase: NN% (current/total)" pattern
  - `git rev-parse --abbrev-ref --symbolic-full-name @{upstream}` checks if branch has tracking upstream
  - Push with `-u remote branch` sets upstream tracking
  - Toolbar buttons use `onContextMenu` for right-click alternative actions (force push, pull strategy)
  - `onOperationProgress` IPC event sends `{ operationId, operation, phase, percent, current, total }`
  - Toast notifications use fixed positioning at top-right with auto-dismiss after 4 seconds
---
## 2026-03-16 15:06 - US-019
- Implemented: Merge feature — merge dialog accessible from toolbar and sidebar branch context menu, with branch picker, merge preview (commit count + fast-forward detection), configurable strategy (auto/no-ff/ff-only), conflict detection with file listing, merge abort
- Files created:
  - `src/renderer/src/components/MergeDialog.tsx` - Full merge dialog with branch selection, preview, strategy options, conflict state with abort button
- Files modified:
  - `src/main/git-service.ts` - Added `getMergePreview()`, `merge()`, `mergeAbort()`, `isMerging()`, `getConflictedFiles()` methods
  - `src/main/git-ipc.ts` - Added IPC handlers for `git:mergePreview`, `git:merge`, `git:mergeAbort`, `git:isMerging`, `git:getConflictedFiles`
  - `src/preload/index.ts` - Added mergePreview, merge, mergeAbort, isMerging, getConflictedFiles to electronAPI.git
  - `src/renderer/src/hooks/useLayoutState.ts` - Updated Window.electronAPI type with new merge methods
  - `src/renderer/src/components/Toolbar.tsx` - Wired Merge toolbar button to open MergeDialog
  - `src/renderer/src/components/Sidebar.tsx` - Replaced placeholder merge handler with MergeDialog integration (preselected branch from context menu)
  - `src/renderer/src/styles/global.css` - Added merge dialog, merge preview, merge strategy, merge conflict section styles
- **Learnings for future iterations:**
  - `git rev-list --count HEAD..branch` counts commits that would be merged
  - `git merge-base HEAD branch` compared to HEAD hash determines if fast-forward is possible
  - `git diff --name-only --diff-filter=U` lists conflicted files
  - MERGE_HEAD file in .git dir indicates a merge is in progress
  - MergeDialog is a standalone reusable component — receives `currentRepo`, `preselectedBranch`, `onClose`, `onMergeComplete` props
  - Merge strategies: no args (auto), `--no-ff` (always merge commit), `--ff-only` (fail if not FF)
  - Sidebar handleMerge now opens MergeDialog instead of calling git:exec directly
---
## 2026-03-16 15:12 - US-020
- Implemented: Rebase feature — standard rebase, interactive rebase with commit action editor, conflict resolution with continue/abort/skip, progress indicator, published commits warning
- Files created:
  - `src/renderer/src/components/RebaseDialog.tsx` - Full rebase dialog with branch picker, preview, interactive commit list (pick/squash/edit/drop/reword/fixup), conflict state with continue/abort/skip
- Files modified:
  - `src/main/git-service.ts` - Added `getRebasePreview()`, `rebase()`, `rebaseInteractive()`, `rebaseContinue()`, `rebaseAbort()`, `rebaseSkip()`, `isRebasing()`, `getRebaseProgress()` methods
  - `src/main/git-ipc.ts` - Added IPC handlers for all rebase operations
  - `src/preload/index.ts` - Added rebasePreview, rebase, rebaseInteractive, rebaseContinue, rebaseAbort, rebaseSkip, isRebasing, rebaseProgress to electronAPI.git
  - `src/renderer/src/hooks/useLayoutState.ts` - Updated Window.electronAPI type with all new rebase methods
  - `src/renderer/src/components/Sidebar.tsx` - Replaced placeholder rebase handler with RebaseDialog integration (preselected branch from context menu)
  - `src/renderer/src/styles/global.css` - Added rebase dialog styles (preview, interactive commit list, conflict section, published warning, progress indicator)
- **Learnings for future iterations:**
  - Interactive rebase uses `GIT_SEQUENCE_EDITOR` env var with a sed script to modify rebase-todo file
  - `rebaseContinue` needs `GIT_EDITOR=true` env var to auto-accept commit messages
  - Rebase progress can be read from `.git/rebase-merge/msgnum` and `end` files (interactive) or `.git/rebase-apply/next` and `last` (non-interactive)
  - `git rev-list --oneline branch..HEAD` lists commits that will be replayed during rebase
  - `git branch -r --contains HEAD` checks if HEAD is reachable from any remote (published commits check)
  - RebaseDialog follows same pattern as MergeDialog — standalone component with `currentRepo`, `preselectedBranch`, `onClose`, `onRebaseComplete` props
---
## 2026-03-16 15:18 - US-021
- Implemented: Cherry-pick feature — single and batch cherry-pick from commit graph context menu, multi-select with Ctrl/Shift+click, conflict detection with continue/abort, success notification with new commit hash
- Files modified:
  - `src/main/git-service.ts` - Added `cherryPick()`, `cherryPickAbort()`, `cherryPickContinue()`, `isCherryPicking()` methods
  - `src/main/git-ipc.ts` - Added IPC handlers for `git:cherryPick`, `git:cherryPickAbort`, `git:cherryPickContinue`, `git:isCherryPicking`
  - `src/preload/index.ts` - Added cherryPick, cherryPickAbort, cherryPickContinue, isCherryPicking to electronAPI.git
  - `src/renderer/src/hooks/useLayoutState.ts` - Updated Window.electronAPI type with new cherry-pick methods
  - `src/renderer/src/components/CommitGraph.tsx` - Added multi-select support (Ctrl+click, Shift+click), wired cherry-pick context menu action, cherry-pick notification UI (success/conflict/picking states), abort/continue/dismiss buttons
  - `src/renderer/src/styles/global.css` - Added cherry-pick notification styles (positioned overlay, conflict file list, action buttons)
- **Learnings for future iterations:**
  - `git cherry-pick` accepts multiple hashes for batch cherry-picking
  - CHERRY_PICK_HEAD file in .git dir indicates a cherry-pick is in progress
  - Cherry-pick --continue doesn't need GIT_EDITOR env var (unlike rebase --continue)
  - `exec()` method in GitService doesn't support `env` option — use `spawn` directly if you need custom env vars
  - Multi-select in CommitGraph uses `selectedHashes` Set<string> alongside single `selectedHash`
  - Ctrl+click toggles individual commits, Shift+click selects range between last selected and clicked
---
## 2026-03-16 15:24 - US-022
- Implemented: Reset (Soft, Mixed, Hard) — dialog accessible from commit graph context menu with mode selection, clear explanations, hard reset requires typing HARD to confirm, success/error feedback
- Files modified:
  - `src/main/git-service.ts` - Added `reset()` method using `git reset --soft/--mixed/--hard <hash>`
  - `src/main/git-ipc.ts` - Added `git:reset` IPC handler
  - `src/preload/index.ts` - Added `reset` to electronAPI.git
  - `src/renderer/src/hooks/useLayoutState.ts` - Updated Window.electronAPI type with reset method
  - `src/renderer/src/components/CommitGraph.tsx` - Wired reset context menu action to open ResetDialog, added resetTarget state
  - `src/renderer/src/styles/global.css` - Added comprehensive ResetDialog styles (overlay, mode cards, hard confirm, error/success)
- Files created:
  - `src/renderer/src/components/ResetDialog.tsx` - Reset dialog with mode selection (soft/mixed/hard), mode descriptions, hard reset confirmation input, auto-close on success
- **Learnings for future iterations:**
  - `git reset --soft/--mixed/--hard <hash>` is straightforward — no special parsing needed
  - ResetDialog follows the same modal overlay pattern as MergeDialog/CloneDialog (z-index: 3000)
  - Hard reset confirmation uses exact string match "HARD" for safety
  - Context menu action wiring: set state in handleContextAction, render dialog conditionally in JSX
---
## 2026-03-16 15:28 - US-023
- Implemented: Revert Commit — right-click commit in graph to revert, merge commit parent selection, conflict resolution with continue/abort, success notification with new commit hash
- Files modified:
  - `src/main/git-service.ts` - Added `revert()`, `revertAbort()`, `revertContinue()`, `isReverting()` methods
  - `src/main/git-ipc.ts` - Added IPC handlers for `git:revert`, `git:revertAbort`, `git:revertContinue`, `git:isReverting`
  - `src/preload/index.ts` - Added revert, revertAbort, revertContinue, isReverting to electronAPI.git
  - `src/renderer/src/hooks/useLayoutState.ts` - Updated Window.electronAPI type with new revert methods
  - `src/renderer/src/components/CommitGraph.tsx` - Wired revert context menu action, added RevertState type, revert/abort/continue handlers, revert notification UI with merge commit parent selection
  - `src/renderer/src/styles/global.css` - Added comprehensive revert notification styles (reverting/success/conflict/merge-prompt states, parent selection buttons, conflict file list)
- **Learnings for future iterations:**
  - `git revert --no-edit <hash>` creates a revert commit without opening editor
  - `git revert -m <parent-number> <hash>` is needed for merge commits (parent 1 or 2)
  - REVERT_HEAD file in .git dir indicates a revert is in progress
  - `exec()` method in GitService doesn't support `env` option — can't pass GIT_EDITOR=true for revert --continue
  - Revert notification UI follows same pattern as cherry-pick notification (positioned overlay in commit-graph-wrapper)
  - RevertState includes 'merge-prompt' status for handling merge commit parent selection
---
## 2026-03-16 15:33 - US-024
- Implemented: Merge Conflict Resolution Tool — visual 3-way conflict resolver with ours/theirs panes, editable result pane, per-conflict and per-file resolution, navigation, and operation continue/abort
- Files created:
  - `src/renderer/src/components/ConflictResolver.tsx` - Full conflict resolution component with:
    - File list sidebar showing all conflicted files with resolved/unresolved status
    - Ours (left) and Theirs (right) read-only panes showing each side's content
    - Editable Result (bottom) textarea for manual editing
    - Conflict marker parser for navigation and per-block resolution
    - Accept Ours / Accept Theirs / Accept Both buttons per conflict block
    - Next Conflict / Previous Conflict navigation with counter
    - Accept All Ours / Accept All Theirs for entire file
    - Mark as Resolved button (disabled when conflict markers remain)
    - Continue operation / Abort operation footer
    - Auto-detects active operation (merge/rebase/cherry-pick/revert)
- Files modified:
  - `src/main/git-service.ts` - Added `getConflictContent()` (reads ours/theirs/base via git show :stage:file), `resolveConflictFile()` (write + git add), `resolveConflictFileWith()` (checkout --ours/--theirs + add), `getActiveOperation()` (detects merge/rebase/cherry-pick/revert)
  - `src/main/git-ipc.ts` - Added IPC handlers for `git:getConflictContent`, `git:resolveConflictFile`, `git:resolveConflictFileWith`, `git:getActiveOperation`
  - `src/preload/index.ts` - Added getConflictContent, resolveConflictFile, resolveConflictFileWith, getActiveOperation to electronAPI.git
  - `src/renderer/src/hooks/useLayoutState.ts` - Updated Window.electronAPI type with new conflict resolution methods
  - `src/renderer/src/components/RepoView.tsx` - Added conflict detection on load, conflict banner, ConflictResolver overlay integration
  - `src/renderer/src/styles/global.css` - Added comprehensive ConflictResolver styles (overlay, file list, 3-pane layout, navigation, conflict buttons, result editor, footer)
- **Learnings for future iterations:**
  - `git show :1:file` = base, `:2:file` = ours, `:3:file` = theirs for conflict content
  - `git checkout --ours/--theirs file` + `git add file` resolves a file with one side
  - Conflict markers: `<<<<<<<` (ours start), `|||||||` (base, optional), `=======` (separator), `>>>>>>>` (theirs end)
  - ConflictResolver is a fixed-position overlay (z-index: 4000) — highest z-index in the app
  - RepoView checks for conflicted files on load and shows banner + auto-opens resolver
  - `getActiveOperation()` checks all operation status methods in parallel to determine what's in progress
---
